# Engine Learnings

Auto-generated by the evolution engine. Tracks patterns, wins, and mistakes across runs.
The engine reads this before every audit to avoid repeating mistakes and amplify what works.

## What Works
- Single-file, targeted prompts with exact function/component names get committed successfully
- ARIA fixes, loading states, and mobile responsiveness are reliable Surgeon mode wins
- Commit messages with issue IDs (e.g. "engine: fix ui-design-system-C1-002") enable accurate verification
- Following existing patterns from CODEBASE_PATTERNS.md prevents type errors and style inconsistencies

## What Fails
- Prompts that reference multiple files at once tend to fail or only partially apply
- Prompts that say "refactor" without specifying exact before/after states produce regressions
- Creating new files without adding proper TypeScript types causes build failures
- Installing npm packages without importing them correctly leads to wasted prompts on build fixes

## Scoring Trends (across 3 runs as of 2026-04-02)
- Run 1 average: ~41/100 (11 modules, 136 commits, crashed at quality gates jq error)
- Run 3 average: ~47/100 (8 modules scored, 35 commits)
- Run 4 average: ~44/100 (11 modules, still Cycle 1 Surgeon mode)
- Scores plateau in 45 to 55 range during Surgeon mode (fixing bugs does not create new capability)
- Real score jumps expected in Architect mode when P0 features from PRODUCTION_ROADMAP.md land

## Rules for Architect Mode (Cycles 4 through 10)
- Read PRODUCTION_ROADMAP.md to find the highest priority unfinished P0 item
- Read CODEBASE_PATTERNS.md to follow exact code patterns (edge functions, hooks, services, migrations)
- One P0 feature per cycle per module. Do not try to build two features in one cycle.
- Create migration files with the next sequential number in supabase/migrations/
- New edge functions go in supabase/functions/<name>/index.ts using Deno + shared auth
- Always run npm run build after creating new files to verify types
- git add supabase/ src/ package.json package-lock.json tsconfig.json

## Common Mistakes to Avoid
- Do NOT recreate existing tables. Check CODEBASE_PATTERNS.md for the full table list.
- Do NOT use CSS modules or styled-components. Inline styles with theme tokens only.
- Do NOT use hyphens in UI text. Use commas or periods.
- Do NOT build offline sync or real-time collaboration from scratch. Both already exist in the codebase.
- Do NOT use floating point for money. Use integer cents.
- Do NOT forget RLS policies on new tables. Every table must have row level security.
