// AUTO-GENERATED — do not edit by hand
// Source: tests/codegen/gen-rls-role-matrix.ts
// Inventory: ops/coverage/table-surface.json (348 tables)
// Tables after framework filter: 348
// Roles: anon, viewer, project_manager, owner
// Ops:   read, insert, update, delete
// Total cells: 5568
//
// Each cell hits real staging Supabase via supabase-js (NOT mocked).
// Concurrency is capped at 20 ops in parallel by vitest's default pool;
// expect a multi-minute run. Skips entirely when staging env vars absent.

import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import {
  SHOULD_RUN,
  ROLES,
  clientForRole,
  admin,
  type RoleName,
} from './auth-helpers'
import type { SupabaseClient } from '@supabase/supabase-js'

// ── Cell results aggregation (so the spec can print a per-role summary) ──
type CellResult = 'allow' | 'deny' | 'error' | 'skip'
const _results: Record<RoleName, Record<string, number>> = {
  anon: { allow: 0, deny: 0, error: 0, skip: 0 },
  viewer: { allow: 0, deny: 0, error: 0, skip: 0 },
  project_manager: { allow: 0, deny: 0, error: 0, skip: 0 },
  owner: { allow: 0, deny: 0, error: 0, skip: 0 },
}

function bucket(role: RoleName, outcome: CellResult): void {
  _results[role][outcome] += 1
}

const TABLES: readonly string[] = ["account_deletion_events","activity_feed","agent_tasks","ai_agent_actions","ai_agents","ai_conversations","ai_cost_tracking","ai_extraction_results","ai_insights","ai_messages","ai_rfi_drafts","ai_training_corrections","ai_usage","api_keys","approval_instances","approval_step_actions","approval_workflow_templates","async_jobs","audit_chain_checkpoints","audit_incidents","audit_log","audit_trail","benchmarks","bid_invitations","bid_packages","bid_responses","bid_submissions","billing_customers","bim_4d_sequence","bim_clash_reports","bim_clashes","bim_element_progress","bim_elements","bim_markups","bim_models","bim_rfi_elements","bim_safety_zones","bonds","budget_items","budget_line_items","budget_snapshots","cancellation_reasons","carbon_factors","certified_payroll_employees","certified_payroll_reports","change_order_line_items","change_orders","chat_messages","citation_interactions","closeout_items","coi_check_in_blocks","coi_expiration_alerts","coi_extractions","coi_requirements","collab_doc_state","commissioning_items","communication_logs","companies","compliance_reports","contract_clauses","contracts","conversations","corrective_actions","cost_codes","cost_database","cost_transactions","crew_attendance","crew_checkins","crew_gps_locations","crew_schedules","crews","custom_reports","daily_log_entries","daily_log_revisions","daily_logs","daily_summaries","deliveries","delivery_items","directory_contacts","document_chunks","document_gen_runs","documents","drafted_action_dedupe","drafted_actions","draw_report_extraction_jobs","drawing_annotations","drawing_bulletins","drawing_classifications","drawing_discrepancies","drawing_distributions","drawing_markups","drawing_pages","drawing_pairs","drawing_revisions","drawing_scopes","drawing_sets","drawing_sheets","drawing_text_content","drawings","dunning_email_log","edit_locks","eeo1_demographics","encrypted_fields","entity_audit_chain","entity_links","equipment","equipment_logs","equipment_maintenance","equipment_rates","estimate_line_items","estimate_rollups","estimates","estimating_items","executive_reports","executor_runs","failed_login_attempts","field_captures","field_session_events","files","financial_periods","ifc_models","impersonation_sessions","import_jobs","inbound_email_replies","inbound_email_unmatched","incidents","inspection_items","insurance_certificates","integration_connections","integration_field_mappings","integration_sync_jobs","integration_sync_log","integrations","invite_logs","invoices","invoices_payable","iris_call_idempotency","iris_grounding_cache","iris_kb_chunks","iris_kb_sources","iris_kb_telemetry","iris_personas","iris_suggestion_history","iris_telemetry","iris_user_personas","iris_voice_diffs","iris_weekly_digests","job_cost_entries","labor_forecasts","labor_rates","leed_credits","lien_waiver_signatures","lien_waivers","magic_link_tokens","material_inventory","material_prices","material_rates","media_links","meeting_action_items","meeting_agenda_items","meeting_attendees","meeting_participants","meeting_series","meetings","message_reactions","notification_preferences","notification_queue","notifications","org_api_token_uses","org_api_tokens","org_branding","org_custom_role_assignments","org_custom_roles","org_s3_export_config","org_sso_config","organism_cycles","organism_experiments","organism_learnings","organism_skills","organization_members","organization_settings","organizations","outbound_email_log","outbound_webhooks","owner_updates","pay_app_reconciliation_lines","pay_app_reconciliations","pay_application_line_items","pay_applications","payapp_audit_overrides","payapp_audit_runs","payapp_owner_preview_comments","payapp_owner_previews","payment_applications","payment_line_items","payment_methods","payment_milestones","payment_transactions","per_project_role_overrides","permit_inspections","permits","photo_comparisons","photo_pin_associations","photo_pins","pilot_agreements","plans","po_line_items","portal_access_tokens","portal_invitations","portal_users","portfolio_projects","portfolios","pre_task_plans","precon_bid_invitations","precon_bid_packages","precon_bid_scope_responses","precon_bid_submissions","precon_scope_items","precon_subcontractors","prequalifications","presence_heartbeats","presence_room_keys","prevailing_wage_decisions","prevailing_wage_rates","profiles","progress_detection_results","project_business_calendar","project_carbon_entries","project_holidays","project_members","project_rfi_custom_fields","project_rfi_notification_prefs","project_rfi_permissions","project_rfi_response_types","project_rfi_settings","project_rfi_workflows","project_role_groups","project_snapshots","project_spec_book","project_templates","projects","punch_item_comments","punch_items","purchase_orders","rate_limit_buckets","rate_limit_overrides","report_runs","report_schedules","report_templates","resend_webhook_events","retainage_entries","retainage_ledger","rfi_assignees","rfi_attachments","rfi_clock_events","rfi_custom_reports","rfi_custom_values","rfi_distributions","rfi_drafts","rfi_drawing_pins","rfi_escalations","rfi_links","rfi_responses","rfi_responses_versions","rfi_saved_views","rfi_scheduled_reports","rfi_user_column_prefs","rfi_watchers","rfis","risk_predictions","role_to_default_persona","safety_certifications","safety_inspection_templates","safety_inspections","safety_observations","safety_photo_analyses","schedule_import_jobs","schedule_of_values","schedule_phases","schedule_recovery_events","scheduled_insights_log","search_index_dirty_flags","signature_fields","site_check_ins","site_map_pins","site_map_zones","site_plans","slack_delivery_log","spec_sections","specifications","sso_configurations","sso_login_events","state_lien_rules","storage_backup_log","stripe_connected_accounts","stripe_processed_events","subcontractor_invoices","subcontractor_ratings","submittal_approvals","submittal_change_history","submittal_distributions","submittal_drawing_pins","submittal_emails","submittal_items","submittal_magic_links","submittal_markup","submittal_packages","submittal_reviewers","submittal_revisions","submittal_saved_views","submittal_settings","submittal_workflow_templates","submittals","subscriptions","sustainability_metrics","takeoff_items","task_templates","tasks","time_entries","timesheets","toolbox_talk_attendees","toolbox_talks","transmittals","typing_indicators","usage_events","user_invitations","user_rfi_notification_overrides","vendor_evaluations","vendors","view_refresh_metadata","walkthrough_captures","walkthrough_sessions","warranties","warranty_claims","waste_logs","weather_cache","weather_records","webhook_deliveries","webhook_endpoints","webhooks","weekly_commitments","wiki_pages","wip_reports","workflow_definitions","workflow_runs","workforce_assignments","workforce_members","zip_upload_jobs"] as const
type TestOp = 'read' | 'insert' | 'update' | 'delete'

// Per-role / per-op clients are resolved once in beforeAll.
const _clients: Partial<Record<RoleName, SupabaseClient>> = {}

beforeAll(async () => {
  if (!SHOULD_RUN) return
  for (const role of ROLES) {
    const c = await clientForRole(role)
    if (c) _clients[role] = c
  }
})

/**
 * Classify a postgrest result against an op.
 *
 * For 'read': empty data + no error  → 'deny' (RLS hid rows)
 *             non-empty data         → 'allow'
 *             error                  → 'deny' if 401/403/PGRST, else 'error'
 *
 * For 'insert'/'update'/'delete':
 *             no error               → 'allow'  (the row was written/deleted)
 *             error PGRST*           → 'deny'   (policy or schema-shape)
 *             other error            → 'error'
 */
function classify(op: TestOp, data: unknown, error: { code?: string; message: string } | null): CellResult {
  if (op === 'read') {
    if (error) {
      const code = error.code ?? ''
      if (/^PGRST|^42|^28|^401|^403/.test(code) || /permission denied|JWT|RLS/i.test(error.message)) {
        return 'deny'
      }
      return 'error'
    }
    if (Array.isArray(data) && data.length === 0) return 'deny'
    return 'allow'
  }
  // write-class
  if (!error) return 'allow'
  const code = error.code ?? ''
  if (/^PGRST|^42|^28|^401|^403/.test(code) || /permission denied|JWT|RLS|new row violates/i.test(error.message)) {
    return 'deny'
  }
  return 'error'
}

describe.skipIf(!SHOULD_RUN)('B.5 — RLS role-matrix (5568 cells)', () => {
  it('inventory matches generator output', () => {
    expect(TABLES.length).toBe(348)
  })

  describe('table: account_deletion_events', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("account_deletion_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'account_deletion_events/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("account_deletion_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("account_deletion_events").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'account_deletion_events/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("account_deletion_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'account_deletion_events/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("account_deletion_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'account_deletion_events/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("account_deletion_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("account_deletion_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("account_deletion_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("account_deletion_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("account_deletion_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("account_deletion_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("account_deletion_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("account_deletion_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("account_deletion_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("account_deletion_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("account_deletion_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("account_deletion_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("account_deletion_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("account_deletion_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("account_deletion_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: activity_feed', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("activity_feed").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'activity_feed/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("activity_feed").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("activity_feed").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'activity_feed/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("activity_feed").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'activity_feed/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("activity_feed").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'activity_feed/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("activity_feed").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("activity_feed").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("activity_feed").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("activity_feed").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("activity_feed").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("activity_feed").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("activity_feed").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("activity_feed").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("activity_feed").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("activity_feed").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("activity_feed").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("activity_feed").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("activity_feed").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("activity_feed").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("activity_feed").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: agent_tasks', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("agent_tasks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'agent_tasks/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("agent_tasks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("agent_tasks").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'agent_tasks/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("agent_tasks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'agent_tasks/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("agent_tasks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'agent_tasks/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("agent_tasks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("agent_tasks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("agent_tasks").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("agent_tasks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("agent_tasks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("agent_tasks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("agent_tasks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("agent_tasks").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("agent_tasks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("agent_tasks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("agent_tasks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("agent_tasks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("agent_tasks").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("agent_tasks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("agent_tasks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: ai_agent_actions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("ai_agent_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_agent_actions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_agent_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_agent_actions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'ai_agent_actions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_agent_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_agent_actions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_agent_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_agent_actions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("ai_agent_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_agent_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_agent_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_agent_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_agent_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("ai_agent_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_agent_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_agent_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_agent_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_agent_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("ai_agent_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_agent_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_agent_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_agent_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_agent_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: ai_agents', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("ai_agents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_agents/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_agents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_agents").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'ai_agents/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_agents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_agents/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_agents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_agents/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("ai_agents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_agents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_agents").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_agents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_agents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("ai_agents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_agents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_agents").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_agents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_agents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("ai_agents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_agents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_agents").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_agents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_agents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: ai_conversations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("ai_conversations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_conversations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_conversations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_conversations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'ai_conversations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_conversations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_conversations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_conversations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_conversations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("ai_conversations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_conversations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_conversations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_conversations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_conversations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("ai_conversations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_conversations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_conversations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_conversations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_conversations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("ai_conversations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_conversations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_conversations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_conversations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_conversations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: ai_cost_tracking', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("ai_cost_tracking").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_cost_tracking/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_cost_tracking").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_cost_tracking").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'ai_cost_tracking/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_cost_tracking").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_cost_tracking/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_cost_tracking").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_cost_tracking/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("ai_cost_tracking").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_cost_tracking").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_cost_tracking").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_cost_tracking").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_cost_tracking").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("ai_cost_tracking").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_cost_tracking").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_cost_tracking").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_cost_tracking").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_cost_tracking").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("ai_cost_tracking").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_cost_tracking").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_cost_tracking").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_cost_tracking").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_cost_tracking").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: ai_extraction_results', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("ai_extraction_results").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_extraction_results/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_extraction_results").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_extraction_results").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'ai_extraction_results/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_extraction_results").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_extraction_results/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_extraction_results").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_extraction_results/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("ai_extraction_results").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_extraction_results").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_extraction_results").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_extraction_results").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_extraction_results").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("ai_extraction_results").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_extraction_results").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_extraction_results").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_extraction_results").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_extraction_results").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("ai_extraction_results").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_extraction_results").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_extraction_results").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_extraction_results").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_extraction_results").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: ai_insights', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("ai_insights").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_insights/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_insights").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_insights").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'ai_insights/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_insights").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_insights/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_insights").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_insights/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("ai_insights").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_insights").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_insights").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_insights").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_insights").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("ai_insights").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_insights").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_insights").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_insights").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_insights").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("ai_insights").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_insights").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_insights").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_insights").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_insights").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: ai_messages', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("ai_messages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_messages/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_messages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_messages").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'ai_messages/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_messages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_messages/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_messages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_messages/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("ai_messages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_messages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_messages").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_messages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_messages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("ai_messages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_messages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_messages").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_messages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_messages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("ai_messages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_messages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_messages").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_messages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_messages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: ai_rfi_drafts', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("ai_rfi_drafts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_rfi_drafts/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_rfi_drafts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_rfi_drafts").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'ai_rfi_drafts/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_rfi_drafts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_rfi_drafts/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_rfi_drafts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_rfi_drafts/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("ai_rfi_drafts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_rfi_drafts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_rfi_drafts").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_rfi_drafts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_rfi_drafts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("ai_rfi_drafts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_rfi_drafts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_rfi_drafts").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_rfi_drafts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_rfi_drafts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("ai_rfi_drafts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_rfi_drafts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_rfi_drafts").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_rfi_drafts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_rfi_drafts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: ai_training_corrections', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("ai_training_corrections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_training_corrections/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_training_corrections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_training_corrections").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'ai_training_corrections/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_training_corrections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_training_corrections/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_training_corrections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_training_corrections/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("ai_training_corrections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_training_corrections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_training_corrections").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_training_corrections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_training_corrections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("ai_training_corrections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_training_corrections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_training_corrections").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_training_corrections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_training_corrections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("ai_training_corrections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_training_corrections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_training_corrections").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_training_corrections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_training_corrections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: ai_usage', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("ai_usage").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_usage/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_usage").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_usage").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'ai_usage/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_usage").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_usage/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_usage").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ai_usage/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("ai_usage").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_usage").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_usage").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_usage").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_usage").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("ai_usage").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_usage").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_usage").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_usage").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_usage").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("ai_usage").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ai_usage").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ai_usage").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ai_usage").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ai_usage").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: api_keys', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("api_keys").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'api_keys/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("api_keys").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("api_keys").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'api_keys/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("api_keys").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'api_keys/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("api_keys").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'api_keys/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("api_keys").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("api_keys").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("api_keys").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("api_keys").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("api_keys").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("api_keys").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("api_keys").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("api_keys").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("api_keys").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("api_keys").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("api_keys").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("api_keys").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("api_keys").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("api_keys").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("api_keys").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: approval_instances', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("approval_instances").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'approval_instances/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_instances").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_instances").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'approval_instances/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_instances").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'approval_instances/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_instances").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'approval_instances/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("approval_instances").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_instances").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_instances").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_instances").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_instances").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("approval_instances").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_instances").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_instances").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_instances").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_instances").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("approval_instances").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_instances").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_instances").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_instances").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_instances").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: approval_step_actions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("approval_step_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'approval_step_actions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_step_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_step_actions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'approval_step_actions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_step_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'approval_step_actions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_step_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'approval_step_actions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("approval_step_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_step_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_step_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_step_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_step_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("approval_step_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_step_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_step_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_step_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_step_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("approval_step_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_step_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_step_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_step_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_step_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: approval_workflow_templates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("approval_workflow_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'approval_workflow_templates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_workflow_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_workflow_templates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'approval_workflow_templates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_workflow_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'approval_workflow_templates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_workflow_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'approval_workflow_templates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("approval_workflow_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_workflow_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_workflow_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_workflow_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_workflow_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("approval_workflow_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_workflow_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_workflow_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_workflow_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_workflow_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("approval_workflow_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("approval_workflow_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("approval_workflow_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("approval_workflow_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("approval_workflow_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: async_jobs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("async_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'async_jobs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("async_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("async_jobs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'async_jobs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("async_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'async_jobs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("async_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'async_jobs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("async_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("async_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("async_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("async_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("async_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("async_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("async_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("async_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("async_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("async_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("async_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("async_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("async_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("async_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("async_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: audit_chain_checkpoints', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("audit_chain_checkpoints").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_chain_checkpoints/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_chain_checkpoints").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_chain_checkpoints").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'audit_chain_checkpoints/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_chain_checkpoints").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_chain_checkpoints/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_chain_checkpoints").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_chain_checkpoints/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("audit_chain_checkpoints").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_chain_checkpoints").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_chain_checkpoints").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_chain_checkpoints").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_chain_checkpoints").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("audit_chain_checkpoints").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_chain_checkpoints").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_chain_checkpoints").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_chain_checkpoints").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_chain_checkpoints").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("audit_chain_checkpoints").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_chain_checkpoints").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_chain_checkpoints").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_chain_checkpoints").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_chain_checkpoints").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: audit_incidents', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("audit_incidents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_incidents/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_incidents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_incidents").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'audit_incidents/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_incidents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_incidents/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_incidents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_incidents/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("audit_incidents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_incidents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_incidents").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_incidents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_incidents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("audit_incidents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_incidents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_incidents").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_incidents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_incidents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("audit_incidents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_incidents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_incidents").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_incidents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_incidents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: audit_log', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("audit_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_log/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_log").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'audit_log/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_log/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_log/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("audit_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("audit_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("audit_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: audit_trail', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("audit_trail").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_trail/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_trail").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_trail").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'audit_trail/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_trail").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_trail/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_trail").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'audit_trail/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("audit_trail").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_trail").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_trail").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_trail").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_trail").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("audit_trail").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_trail").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_trail").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_trail").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_trail").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("audit_trail").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("audit_trail").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("audit_trail").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("audit_trail").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("audit_trail").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: benchmarks', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("benchmarks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'benchmarks/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("benchmarks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("benchmarks").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'benchmarks/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("benchmarks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'benchmarks/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("benchmarks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'benchmarks/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("benchmarks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("benchmarks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("benchmarks").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("benchmarks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("benchmarks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("benchmarks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("benchmarks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("benchmarks").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("benchmarks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("benchmarks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("benchmarks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("benchmarks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("benchmarks").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("benchmarks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("benchmarks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bid_invitations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bid_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_invitations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_invitations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bid_invitations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_invitations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_invitations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bid_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bid_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bid_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bid_packages', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bid_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_packages/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_packages").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bid_packages/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_packages/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_packages/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bid_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_packages").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bid_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_packages").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bid_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_packages").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bid_responses', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bid_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_responses/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_responses").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bid_responses/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_responses/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_responses/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bid_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_responses").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bid_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_responses").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bid_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_responses").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bid_submissions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bid_submissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_submissions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_submissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_submissions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bid_submissions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_submissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_submissions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_submissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bid_submissions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bid_submissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_submissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_submissions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_submissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_submissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bid_submissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_submissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_submissions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_submissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_submissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bid_submissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bid_submissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bid_submissions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bid_submissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bid_submissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: billing_customers', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("billing_customers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'billing_customers/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("billing_customers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("billing_customers").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'billing_customers/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("billing_customers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'billing_customers/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("billing_customers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'billing_customers/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("billing_customers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("billing_customers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("billing_customers").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("billing_customers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("billing_customers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("billing_customers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("billing_customers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("billing_customers").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("billing_customers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("billing_customers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("billing_customers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("billing_customers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("billing_customers").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("billing_customers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("billing_customers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bim_4d_sequence', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bim_4d_sequence").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_4d_sequence/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_4d_sequence").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_4d_sequence").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bim_4d_sequence/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_4d_sequence").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_4d_sequence/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_4d_sequence").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_4d_sequence/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bim_4d_sequence").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_4d_sequence").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_4d_sequence").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_4d_sequence").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_4d_sequence").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bim_4d_sequence").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_4d_sequence").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_4d_sequence").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_4d_sequence").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_4d_sequence").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bim_4d_sequence").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_4d_sequence").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_4d_sequence").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_4d_sequence").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_4d_sequence").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bim_clash_reports', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bim_clash_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_clash_reports/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_clash_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_clash_reports").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bim_clash_reports/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_clash_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_clash_reports/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_clash_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_clash_reports/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bim_clash_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_clash_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_clash_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_clash_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_clash_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bim_clash_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_clash_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_clash_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_clash_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_clash_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bim_clash_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_clash_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_clash_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_clash_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_clash_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bim_clashes', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bim_clashes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_clashes/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_clashes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_clashes").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bim_clashes/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_clashes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_clashes/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_clashes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_clashes/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bim_clashes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_clashes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_clashes").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_clashes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_clashes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bim_clashes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_clashes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_clashes").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_clashes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_clashes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bim_clashes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_clashes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_clashes").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_clashes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_clashes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bim_element_progress', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bim_element_progress").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_element_progress/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_element_progress").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_element_progress").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bim_element_progress/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_element_progress").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_element_progress/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_element_progress").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_element_progress/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bim_element_progress").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_element_progress").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_element_progress").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_element_progress").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_element_progress").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bim_element_progress").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_element_progress").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_element_progress").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_element_progress").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_element_progress").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bim_element_progress").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_element_progress").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_element_progress").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_element_progress").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_element_progress").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bim_elements', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bim_elements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_elements/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_elements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_elements").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bim_elements/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_elements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_elements/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_elements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_elements/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bim_elements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_elements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_elements").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_elements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_elements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bim_elements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_elements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_elements").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_elements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_elements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bim_elements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_elements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_elements").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_elements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_elements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bim_markups', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bim_markups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_markups/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_markups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_markups").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bim_markups/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_markups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_markups/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_markups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_markups/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bim_markups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_markups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_markups").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_markups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_markups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bim_markups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_markups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_markups").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_markups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_markups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bim_markups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_markups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_markups").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_markups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_markups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bim_models', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bim_models").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_models/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_models").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_models").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bim_models/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_models").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_models/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_models").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_models/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bim_models").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_models").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_models").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_models").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_models").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bim_models").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_models").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_models").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_models").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_models").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bim_models").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_models").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_models").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_models").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_models").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bim_rfi_elements', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bim_rfi_elements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_rfi_elements/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_rfi_elements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_rfi_elements").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bim_rfi_elements/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_rfi_elements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_rfi_elements/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_rfi_elements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_rfi_elements/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bim_rfi_elements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_rfi_elements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_rfi_elements").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_rfi_elements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_rfi_elements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bim_rfi_elements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_rfi_elements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_rfi_elements").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_rfi_elements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_rfi_elements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bim_rfi_elements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_rfi_elements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_rfi_elements").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_rfi_elements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_rfi_elements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bim_safety_zones', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bim_safety_zones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_safety_zones/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_safety_zones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_safety_zones").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bim_safety_zones/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_safety_zones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_safety_zones/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_safety_zones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bim_safety_zones/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bim_safety_zones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_safety_zones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_safety_zones").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_safety_zones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_safety_zones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bim_safety_zones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_safety_zones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_safety_zones").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_safety_zones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_safety_zones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bim_safety_zones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bim_safety_zones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bim_safety_zones").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bim_safety_zones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bim_safety_zones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: bonds', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("bonds").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bonds/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bonds").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bonds").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'bonds/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bonds").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bonds/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bonds").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'bonds/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("bonds").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bonds").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bonds").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bonds").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bonds").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("bonds").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bonds").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bonds").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bonds").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bonds").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("bonds").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("bonds").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("bonds").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("bonds").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("bonds").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: budget_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("budget_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'budget_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'budget_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'budget_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'budget_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("budget_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("budget_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("budget_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: budget_line_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("budget_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'budget_line_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_line_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'budget_line_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'budget_line_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'budget_line_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("budget_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("budget_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("budget_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: budget_snapshots', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("budget_snapshots").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'budget_snapshots/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_snapshots").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_snapshots").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'budget_snapshots/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_snapshots").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'budget_snapshots/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_snapshots").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'budget_snapshots/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("budget_snapshots").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_snapshots").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_snapshots").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_snapshots").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_snapshots").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("budget_snapshots").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_snapshots").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_snapshots").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_snapshots").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_snapshots").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("budget_snapshots").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("budget_snapshots").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("budget_snapshots").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("budget_snapshots").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("budget_snapshots").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: cancellation_reasons', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("cancellation_reasons").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cancellation_reasons/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cancellation_reasons").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cancellation_reasons").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'cancellation_reasons/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cancellation_reasons").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cancellation_reasons/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cancellation_reasons").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cancellation_reasons/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("cancellation_reasons").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cancellation_reasons").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cancellation_reasons").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cancellation_reasons").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cancellation_reasons").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("cancellation_reasons").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cancellation_reasons").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cancellation_reasons").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cancellation_reasons").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cancellation_reasons").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("cancellation_reasons").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cancellation_reasons").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cancellation_reasons").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cancellation_reasons").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cancellation_reasons").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: carbon_factors', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("carbon_factors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'carbon_factors/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("carbon_factors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("carbon_factors").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'carbon_factors/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("carbon_factors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'carbon_factors/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("carbon_factors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'carbon_factors/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("carbon_factors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("carbon_factors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("carbon_factors").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("carbon_factors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("carbon_factors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("carbon_factors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("carbon_factors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("carbon_factors").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("carbon_factors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("carbon_factors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("carbon_factors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("carbon_factors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("carbon_factors").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("carbon_factors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("carbon_factors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: certified_payroll_employees', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("certified_payroll_employees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'certified_payroll_employees/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("certified_payroll_employees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("certified_payroll_employees").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'certified_payroll_employees/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("certified_payroll_employees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'certified_payroll_employees/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("certified_payroll_employees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'certified_payroll_employees/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("certified_payroll_employees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("certified_payroll_employees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("certified_payroll_employees").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("certified_payroll_employees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("certified_payroll_employees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("certified_payroll_employees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("certified_payroll_employees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("certified_payroll_employees").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("certified_payroll_employees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("certified_payroll_employees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("certified_payroll_employees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("certified_payroll_employees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("certified_payroll_employees").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("certified_payroll_employees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("certified_payroll_employees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: certified_payroll_reports', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("certified_payroll_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'certified_payroll_reports/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("certified_payroll_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("certified_payroll_reports").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'certified_payroll_reports/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("certified_payroll_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'certified_payroll_reports/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("certified_payroll_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'certified_payroll_reports/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("certified_payroll_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("certified_payroll_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("certified_payroll_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("certified_payroll_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("certified_payroll_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("certified_payroll_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("certified_payroll_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("certified_payroll_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("certified_payroll_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("certified_payroll_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("certified_payroll_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("certified_payroll_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("certified_payroll_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("certified_payroll_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("certified_payroll_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: change_order_line_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("change_order_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'change_order_line_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("change_order_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("change_order_line_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'change_order_line_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("change_order_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'change_order_line_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("change_order_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'change_order_line_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("change_order_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("change_order_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("change_order_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("change_order_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("change_order_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("change_order_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("change_order_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("change_order_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("change_order_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("change_order_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("change_order_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("change_order_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("change_order_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("change_order_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("change_order_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: change_orders', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("change_orders").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'change_orders/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("change_orders").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("change_orders").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'change_orders/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("change_orders").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'change_orders/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("change_orders").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'change_orders/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("change_orders").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("change_orders").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("change_orders").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("change_orders").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("change_orders").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("change_orders").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("change_orders").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("change_orders").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("change_orders").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("change_orders").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("change_orders").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("change_orders").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("change_orders").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("change_orders").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("change_orders").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: chat_messages', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("chat_messages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'chat_messages/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("chat_messages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("chat_messages").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'chat_messages/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("chat_messages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'chat_messages/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("chat_messages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'chat_messages/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("chat_messages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("chat_messages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("chat_messages").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("chat_messages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("chat_messages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("chat_messages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("chat_messages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("chat_messages").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("chat_messages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("chat_messages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("chat_messages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("chat_messages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("chat_messages").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("chat_messages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("chat_messages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: citation_interactions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("citation_interactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'citation_interactions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("citation_interactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("citation_interactions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'citation_interactions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("citation_interactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'citation_interactions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("citation_interactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'citation_interactions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("citation_interactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("citation_interactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("citation_interactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("citation_interactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("citation_interactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("citation_interactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("citation_interactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("citation_interactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("citation_interactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("citation_interactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("citation_interactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("citation_interactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("citation_interactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("citation_interactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("citation_interactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: closeout_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("closeout_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'closeout_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("closeout_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("closeout_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'closeout_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("closeout_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'closeout_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("closeout_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'closeout_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("closeout_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("closeout_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("closeout_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("closeout_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("closeout_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("closeout_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("closeout_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("closeout_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("closeout_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("closeout_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("closeout_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("closeout_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("closeout_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("closeout_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("closeout_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: coi_check_in_blocks', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("coi_check_in_blocks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_check_in_blocks/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_check_in_blocks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_check_in_blocks").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'coi_check_in_blocks/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_check_in_blocks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_check_in_blocks/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_check_in_blocks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_check_in_blocks/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("coi_check_in_blocks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_check_in_blocks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_check_in_blocks").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_check_in_blocks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_check_in_blocks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("coi_check_in_blocks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_check_in_blocks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_check_in_blocks").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_check_in_blocks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_check_in_blocks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("coi_check_in_blocks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_check_in_blocks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_check_in_blocks").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_check_in_blocks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_check_in_blocks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: coi_expiration_alerts', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("coi_expiration_alerts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_expiration_alerts/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_expiration_alerts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_expiration_alerts").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'coi_expiration_alerts/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_expiration_alerts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_expiration_alerts/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_expiration_alerts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_expiration_alerts/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("coi_expiration_alerts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_expiration_alerts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_expiration_alerts").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_expiration_alerts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_expiration_alerts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("coi_expiration_alerts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_expiration_alerts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_expiration_alerts").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_expiration_alerts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_expiration_alerts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("coi_expiration_alerts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_expiration_alerts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_expiration_alerts").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_expiration_alerts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_expiration_alerts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: coi_extractions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("coi_extractions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_extractions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_extractions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_extractions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'coi_extractions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_extractions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_extractions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_extractions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_extractions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("coi_extractions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_extractions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_extractions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_extractions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_extractions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("coi_extractions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_extractions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_extractions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_extractions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_extractions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("coi_extractions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_extractions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_extractions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_extractions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_extractions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: coi_requirements', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("coi_requirements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_requirements/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_requirements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_requirements").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'coi_requirements/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_requirements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_requirements/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_requirements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'coi_requirements/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("coi_requirements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_requirements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_requirements").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_requirements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_requirements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("coi_requirements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_requirements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_requirements").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_requirements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_requirements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("coi_requirements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("coi_requirements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("coi_requirements").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("coi_requirements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("coi_requirements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: collab_doc_state', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("collab_doc_state").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'collab_doc_state/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("collab_doc_state").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("collab_doc_state").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'collab_doc_state/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("collab_doc_state").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'collab_doc_state/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("collab_doc_state").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'collab_doc_state/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("collab_doc_state").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("collab_doc_state").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("collab_doc_state").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("collab_doc_state").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("collab_doc_state").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("collab_doc_state").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("collab_doc_state").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("collab_doc_state").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("collab_doc_state").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("collab_doc_state").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("collab_doc_state").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("collab_doc_state").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("collab_doc_state").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("collab_doc_state").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("collab_doc_state").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: commissioning_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("commissioning_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'commissioning_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("commissioning_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("commissioning_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'commissioning_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("commissioning_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'commissioning_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("commissioning_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'commissioning_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("commissioning_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("commissioning_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("commissioning_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("commissioning_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("commissioning_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("commissioning_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("commissioning_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("commissioning_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("commissioning_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("commissioning_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("commissioning_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("commissioning_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("commissioning_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("commissioning_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("commissioning_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: communication_logs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("communication_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'communication_logs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("communication_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("communication_logs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'communication_logs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("communication_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'communication_logs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("communication_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'communication_logs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("communication_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("communication_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("communication_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("communication_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("communication_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("communication_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("communication_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("communication_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("communication_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("communication_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("communication_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("communication_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("communication_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("communication_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("communication_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: companies', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("companies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'companies/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("companies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("companies").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'companies/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("companies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'companies/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("companies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'companies/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("companies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("companies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("companies").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("companies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("companies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("companies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("companies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("companies").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("companies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("companies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("companies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("companies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("companies").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("companies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("companies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: compliance_reports', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("compliance_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'compliance_reports/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("compliance_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("compliance_reports").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'compliance_reports/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("compliance_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'compliance_reports/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("compliance_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'compliance_reports/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("compliance_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("compliance_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("compliance_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("compliance_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("compliance_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("compliance_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("compliance_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("compliance_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("compliance_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("compliance_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("compliance_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("compliance_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("compliance_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("compliance_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("compliance_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: contract_clauses', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("contract_clauses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'contract_clauses/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("contract_clauses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("contract_clauses").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'contract_clauses/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("contract_clauses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'contract_clauses/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("contract_clauses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'contract_clauses/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("contract_clauses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("contract_clauses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("contract_clauses").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("contract_clauses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("contract_clauses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("contract_clauses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("contract_clauses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("contract_clauses").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("contract_clauses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("contract_clauses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("contract_clauses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("contract_clauses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("contract_clauses").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("contract_clauses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("contract_clauses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: contracts', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("contracts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'contracts/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("contracts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("contracts").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'contracts/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("contracts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'contracts/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("contracts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'contracts/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("contracts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("contracts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("contracts").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("contracts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("contracts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("contracts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("contracts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("contracts").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("contracts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("contracts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("contracts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("contracts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("contracts").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("contracts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("contracts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: conversations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("conversations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'conversations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("conversations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("conversations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'conversations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("conversations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'conversations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("conversations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'conversations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("conversations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("conversations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("conversations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("conversations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("conversations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("conversations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("conversations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("conversations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("conversations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("conversations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("conversations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("conversations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("conversations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("conversations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("conversations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: corrective_actions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("corrective_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'corrective_actions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("corrective_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("corrective_actions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'corrective_actions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("corrective_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'corrective_actions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("corrective_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'corrective_actions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("corrective_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("corrective_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("corrective_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("corrective_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("corrective_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("corrective_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("corrective_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("corrective_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("corrective_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("corrective_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("corrective_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("corrective_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("corrective_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("corrective_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("corrective_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: cost_codes', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("cost_codes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cost_codes/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_codes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_codes").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'cost_codes/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_codes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cost_codes/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_codes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cost_codes/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("cost_codes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_codes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_codes").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_codes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_codes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("cost_codes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_codes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_codes").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_codes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_codes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("cost_codes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_codes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_codes").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_codes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_codes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: cost_database', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("cost_database").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cost_database/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_database").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_database").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'cost_database/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_database").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cost_database/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_database").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cost_database/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("cost_database").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_database").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_database").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_database").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_database").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("cost_database").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_database").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_database").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_database").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_database").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("cost_database").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_database").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_database").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_database").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_database").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: cost_transactions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("cost_transactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cost_transactions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_transactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_transactions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'cost_transactions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_transactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cost_transactions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_transactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'cost_transactions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("cost_transactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_transactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_transactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_transactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_transactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("cost_transactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_transactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_transactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_transactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_transactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("cost_transactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("cost_transactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("cost_transactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("cost_transactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("cost_transactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: crew_attendance', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("crew_attendance").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_attendance/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_attendance").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_attendance").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'crew_attendance/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_attendance").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_attendance/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_attendance").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_attendance/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("crew_attendance").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_attendance").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_attendance").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_attendance").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_attendance").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("crew_attendance").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_attendance").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_attendance").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_attendance").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_attendance").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("crew_attendance").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_attendance").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_attendance").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_attendance").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_attendance").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: crew_checkins', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("crew_checkins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_checkins/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_checkins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_checkins").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'crew_checkins/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_checkins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_checkins/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_checkins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_checkins/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("crew_checkins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_checkins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_checkins").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_checkins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_checkins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("crew_checkins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_checkins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_checkins").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_checkins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_checkins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("crew_checkins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_checkins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_checkins").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_checkins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_checkins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: crew_gps_locations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("crew_gps_locations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_gps_locations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_gps_locations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_gps_locations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'crew_gps_locations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_gps_locations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_gps_locations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_gps_locations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_gps_locations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("crew_gps_locations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_gps_locations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_gps_locations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_gps_locations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_gps_locations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("crew_gps_locations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_gps_locations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_gps_locations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_gps_locations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_gps_locations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("crew_gps_locations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_gps_locations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_gps_locations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_gps_locations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_gps_locations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: crew_schedules', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("crew_schedules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_schedules/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_schedules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_schedules").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'crew_schedules/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_schedules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_schedules/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_schedules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crew_schedules/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("crew_schedules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_schedules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_schedules").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_schedules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_schedules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("crew_schedules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_schedules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_schedules").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_schedules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_schedules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("crew_schedules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crew_schedules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crew_schedules").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crew_schedules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crew_schedules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: crews', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("crews").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crews/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crews").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crews").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'crews/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crews").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crews/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crews").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'crews/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("crews").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crews").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crews").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crews").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crews").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("crews").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crews").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crews").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crews").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crews").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("crews").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("crews").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("crews").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("crews").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("crews").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: custom_reports', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("custom_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'custom_reports/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("custom_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("custom_reports").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'custom_reports/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("custom_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'custom_reports/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("custom_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'custom_reports/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("custom_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("custom_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("custom_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("custom_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("custom_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("custom_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("custom_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("custom_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("custom_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("custom_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("custom_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("custom_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("custom_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("custom_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("custom_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: daily_log_entries', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("daily_log_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_log_entries/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_log_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_log_entries").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'daily_log_entries/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_log_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_log_entries/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_log_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_log_entries/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("daily_log_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_log_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_log_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_log_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_log_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("daily_log_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_log_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_log_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_log_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_log_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("daily_log_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_log_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_log_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_log_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_log_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: daily_log_revisions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("daily_log_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_log_revisions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_log_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_log_revisions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'daily_log_revisions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_log_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_log_revisions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_log_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_log_revisions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("daily_log_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_log_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_log_revisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_log_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_log_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("daily_log_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_log_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_log_revisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_log_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_log_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("daily_log_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_log_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_log_revisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_log_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_log_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: daily_logs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("daily_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_logs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_logs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'daily_logs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_logs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_logs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("daily_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("daily_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("daily_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: daily_summaries', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("daily_summaries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_summaries/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_summaries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_summaries").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'daily_summaries/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_summaries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_summaries/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_summaries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'daily_summaries/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("daily_summaries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_summaries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_summaries").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_summaries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_summaries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("daily_summaries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_summaries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_summaries").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_summaries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_summaries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("daily_summaries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("daily_summaries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("daily_summaries").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("daily_summaries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("daily_summaries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: deliveries', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("deliveries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'deliveries/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("deliveries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("deliveries").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'deliveries/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("deliveries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'deliveries/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("deliveries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'deliveries/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("deliveries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("deliveries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("deliveries").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("deliveries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("deliveries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("deliveries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("deliveries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("deliveries").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("deliveries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("deliveries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("deliveries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("deliveries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("deliveries").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("deliveries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("deliveries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: delivery_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("delivery_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'delivery_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("delivery_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("delivery_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'delivery_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("delivery_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'delivery_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("delivery_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'delivery_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("delivery_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("delivery_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("delivery_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("delivery_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("delivery_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("delivery_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("delivery_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("delivery_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("delivery_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("delivery_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("delivery_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("delivery_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("delivery_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("delivery_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("delivery_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: directory_contacts', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("directory_contacts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'directory_contacts/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("directory_contacts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("directory_contacts").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'directory_contacts/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("directory_contacts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'directory_contacts/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("directory_contacts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'directory_contacts/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("directory_contacts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("directory_contacts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("directory_contacts").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("directory_contacts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("directory_contacts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("directory_contacts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("directory_contacts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("directory_contacts").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("directory_contacts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("directory_contacts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("directory_contacts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("directory_contacts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("directory_contacts").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("directory_contacts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("directory_contacts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: document_chunks', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("document_chunks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'document_chunks/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("document_chunks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("document_chunks").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'document_chunks/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("document_chunks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'document_chunks/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("document_chunks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'document_chunks/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("document_chunks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("document_chunks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("document_chunks").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("document_chunks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("document_chunks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("document_chunks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("document_chunks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("document_chunks").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("document_chunks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("document_chunks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("document_chunks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("document_chunks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("document_chunks").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("document_chunks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("document_chunks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: document_gen_runs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("document_gen_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'document_gen_runs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("document_gen_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("document_gen_runs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'document_gen_runs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("document_gen_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'document_gen_runs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("document_gen_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'document_gen_runs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("document_gen_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("document_gen_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("document_gen_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("document_gen_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("document_gen_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("document_gen_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("document_gen_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("document_gen_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("document_gen_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("document_gen_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("document_gen_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("document_gen_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("document_gen_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("document_gen_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("document_gen_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: documents', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("documents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'documents/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("documents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("documents").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'documents/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("documents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'documents/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("documents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'documents/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("documents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("documents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("documents").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("documents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("documents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("documents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("documents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("documents").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("documents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("documents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("documents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("documents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("documents").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("documents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("documents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drafted_action_dedupe', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drafted_action_dedupe").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drafted_action_dedupe/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drafted_action_dedupe").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drafted_action_dedupe").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drafted_action_dedupe/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drafted_action_dedupe").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drafted_action_dedupe/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drafted_action_dedupe").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drafted_action_dedupe/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drafted_action_dedupe").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drafted_action_dedupe").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drafted_action_dedupe").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drafted_action_dedupe").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drafted_action_dedupe").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drafted_action_dedupe").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drafted_action_dedupe").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drafted_action_dedupe").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drafted_action_dedupe").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drafted_action_dedupe").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drafted_action_dedupe").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drafted_action_dedupe").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drafted_action_dedupe").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drafted_action_dedupe").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drafted_action_dedupe").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drafted_actions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drafted_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drafted_actions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drafted_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drafted_actions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drafted_actions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drafted_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drafted_actions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drafted_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drafted_actions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drafted_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drafted_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drafted_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drafted_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drafted_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drafted_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drafted_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drafted_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drafted_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drafted_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drafted_actions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drafted_actions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drafted_actions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drafted_actions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drafted_actions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: draw_report_extraction_jobs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("draw_report_extraction_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'draw_report_extraction_jobs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("draw_report_extraction_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("draw_report_extraction_jobs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'draw_report_extraction_jobs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("draw_report_extraction_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'draw_report_extraction_jobs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("draw_report_extraction_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'draw_report_extraction_jobs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("draw_report_extraction_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("draw_report_extraction_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("draw_report_extraction_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("draw_report_extraction_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("draw_report_extraction_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("draw_report_extraction_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("draw_report_extraction_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("draw_report_extraction_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("draw_report_extraction_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("draw_report_extraction_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("draw_report_extraction_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("draw_report_extraction_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("draw_report_extraction_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("draw_report_extraction_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("draw_report_extraction_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_annotations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_annotations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_annotations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_annotations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_annotations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_annotations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_annotations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_annotations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_annotations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_annotations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_annotations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_annotations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_annotations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_annotations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_annotations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_annotations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_annotations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_annotations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_annotations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_annotations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_annotations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_annotations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_annotations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_annotations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_annotations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_bulletins', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_bulletins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_bulletins/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_bulletins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_bulletins").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_bulletins/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_bulletins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_bulletins/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_bulletins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_bulletins/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_bulletins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_bulletins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_bulletins").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_bulletins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_bulletins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_bulletins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_bulletins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_bulletins").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_bulletins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_bulletins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_bulletins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_bulletins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_bulletins").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_bulletins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_bulletins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_classifications', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_classifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_classifications/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_classifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_classifications").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_classifications/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_classifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_classifications/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_classifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_classifications/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_classifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_classifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_classifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_classifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_classifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_classifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_classifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_classifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_classifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_classifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_classifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_classifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_classifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_classifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_classifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_discrepancies', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_discrepancies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_discrepancies/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_discrepancies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_discrepancies").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_discrepancies/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_discrepancies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_discrepancies/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_discrepancies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_discrepancies/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_discrepancies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_discrepancies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_discrepancies").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_discrepancies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_discrepancies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_discrepancies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_discrepancies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_discrepancies").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_discrepancies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_discrepancies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_discrepancies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_discrepancies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_discrepancies").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_discrepancies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_discrepancies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_distributions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_distributions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_distributions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_distributions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_distributions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_distributions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_distributions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_distributions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_distributions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_markups', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_markups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_markups/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_markups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_markups").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_markups/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_markups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_markups/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_markups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_markups/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_markups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_markups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_markups").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_markups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_markups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_markups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_markups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_markups").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_markups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_markups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_markups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_markups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_markups").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_markups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_markups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_pages', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_pages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_pages/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_pages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_pages").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_pages/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_pages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_pages/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_pages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_pages/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_pages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_pages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_pages").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_pages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_pages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_pages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_pages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_pages").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_pages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_pages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_pages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_pages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_pages").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_pages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_pages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_pairs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_pairs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_pairs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_pairs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_pairs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_pairs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_pairs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_pairs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_pairs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_pairs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_pairs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_pairs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_pairs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_pairs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_pairs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_pairs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_pairs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_pairs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_pairs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_pairs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_pairs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_pairs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_pairs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_pairs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_pairs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_revisions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_revisions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_revisions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_revisions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_revisions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_revisions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_revisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_revisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_revisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_scopes', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_scopes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_scopes/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_scopes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_scopes").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_scopes/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_scopes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_scopes/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_scopes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_scopes/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_scopes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_scopes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_scopes").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_scopes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_scopes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_scopes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_scopes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_scopes").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_scopes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_scopes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_scopes").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_scopes").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_scopes").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_scopes").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_scopes").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_sets', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_sets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_sets/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_sets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_sets").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_sets/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_sets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_sets/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_sets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_sets/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_sets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_sets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_sets").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_sets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_sets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_sets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_sets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_sets").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_sets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_sets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_sets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_sets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_sets").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_sets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_sets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_sheets', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_sheets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_sheets/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_sheets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_sheets").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_sheets/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_sheets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_sheets/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_sheets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_sheets/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_sheets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_sheets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_sheets").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_sheets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_sheets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_sheets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_sheets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_sheets").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_sheets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_sheets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_sheets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_sheets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_sheets").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_sheets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_sheets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawing_text_content', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawing_text_content").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_text_content/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_text_content").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_text_content").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawing_text_content/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_text_content").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_text_content/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_text_content").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawing_text_content/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawing_text_content").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_text_content").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_text_content").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_text_content").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_text_content").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawing_text_content").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_text_content").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_text_content").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_text_content").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_text_content").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawing_text_content").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawing_text_content").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawing_text_content").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawing_text_content").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawing_text_content").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: drawings', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("drawings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawings/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawings").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'drawings/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawings/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'drawings/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("drawings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawings").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("drawings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawings").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("drawings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("drawings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("drawings").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("drawings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("drawings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: dunning_email_log', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("dunning_email_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'dunning_email_log/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("dunning_email_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("dunning_email_log").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'dunning_email_log/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("dunning_email_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'dunning_email_log/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("dunning_email_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'dunning_email_log/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("dunning_email_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("dunning_email_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("dunning_email_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("dunning_email_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("dunning_email_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("dunning_email_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("dunning_email_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("dunning_email_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("dunning_email_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("dunning_email_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("dunning_email_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("dunning_email_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("dunning_email_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("dunning_email_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("dunning_email_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: edit_locks', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("edit_locks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'edit_locks/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("edit_locks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("edit_locks").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'edit_locks/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("edit_locks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'edit_locks/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("edit_locks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'edit_locks/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("edit_locks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("edit_locks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("edit_locks").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("edit_locks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("edit_locks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("edit_locks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("edit_locks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("edit_locks").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("edit_locks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("edit_locks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("edit_locks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("edit_locks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("edit_locks").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("edit_locks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("edit_locks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: eeo1_demographics', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("eeo1_demographics").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'eeo1_demographics/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("eeo1_demographics").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("eeo1_demographics").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'eeo1_demographics/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("eeo1_demographics").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'eeo1_demographics/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("eeo1_demographics").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'eeo1_demographics/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("eeo1_demographics").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("eeo1_demographics").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("eeo1_demographics").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("eeo1_demographics").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("eeo1_demographics").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("eeo1_demographics").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("eeo1_demographics").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("eeo1_demographics").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("eeo1_demographics").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("eeo1_demographics").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("eeo1_demographics").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("eeo1_demographics").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("eeo1_demographics").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("eeo1_demographics").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("eeo1_demographics").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: encrypted_fields', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("encrypted_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'encrypted_fields/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("encrypted_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("encrypted_fields").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'encrypted_fields/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("encrypted_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'encrypted_fields/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("encrypted_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'encrypted_fields/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("encrypted_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("encrypted_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("encrypted_fields").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("encrypted_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("encrypted_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("encrypted_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("encrypted_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("encrypted_fields").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("encrypted_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("encrypted_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("encrypted_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("encrypted_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("encrypted_fields").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("encrypted_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("encrypted_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: entity_audit_chain', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("entity_audit_chain").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'entity_audit_chain/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("entity_audit_chain").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("entity_audit_chain").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'entity_audit_chain/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("entity_audit_chain").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'entity_audit_chain/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("entity_audit_chain").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'entity_audit_chain/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("entity_audit_chain").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("entity_audit_chain").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("entity_audit_chain").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("entity_audit_chain").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("entity_audit_chain").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("entity_audit_chain").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("entity_audit_chain").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("entity_audit_chain").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("entity_audit_chain").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("entity_audit_chain").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("entity_audit_chain").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("entity_audit_chain").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("entity_audit_chain").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("entity_audit_chain").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("entity_audit_chain").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: entity_links', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("entity_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'entity_links/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("entity_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("entity_links").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'entity_links/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("entity_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'entity_links/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("entity_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'entity_links/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("entity_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("entity_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("entity_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("entity_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("entity_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("entity_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("entity_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("entity_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("entity_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("entity_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("entity_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("entity_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("entity_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("entity_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("entity_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: equipment', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("equipment").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'equipment/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("equipment").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("equipment").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("equipment").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: equipment_logs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("equipment_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment_logs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_logs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'equipment_logs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment_logs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment_logs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("equipment_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("equipment_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("equipment_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: equipment_maintenance', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("equipment_maintenance").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment_maintenance/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_maintenance").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_maintenance").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'equipment_maintenance/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_maintenance").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment_maintenance/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_maintenance").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment_maintenance/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("equipment_maintenance").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_maintenance").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_maintenance").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_maintenance").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_maintenance").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("equipment_maintenance").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_maintenance").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_maintenance").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_maintenance").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_maintenance").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("equipment_maintenance").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_maintenance").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_maintenance").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_maintenance").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_maintenance").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: equipment_rates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("equipment_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment_rates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_rates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'equipment_rates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment_rates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'equipment_rates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("equipment_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("equipment_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("equipment_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("equipment_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("equipment_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("equipment_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("equipment_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: estimate_line_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("estimate_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimate_line_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimate_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimate_line_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'estimate_line_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimate_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimate_line_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimate_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimate_line_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("estimate_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimate_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimate_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimate_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimate_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("estimate_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimate_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimate_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimate_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimate_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("estimate_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimate_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimate_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimate_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimate_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: estimate_rollups', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("estimate_rollups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimate_rollups/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimate_rollups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimate_rollups").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'estimate_rollups/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimate_rollups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimate_rollups/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimate_rollups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimate_rollups/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("estimate_rollups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimate_rollups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimate_rollups").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimate_rollups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimate_rollups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("estimate_rollups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimate_rollups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimate_rollups").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimate_rollups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimate_rollups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("estimate_rollups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimate_rollups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimate_rollups").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimate_rollups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimate_rollups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: estimates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("estimates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'estimates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("estimates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("estimates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("estimates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: estimating_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("estimating_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimating_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimating_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimating_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'estimating_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimating_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimating_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimating_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'estimating_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("estimating_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimating_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimating_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimating_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimating_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("estimating_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimating_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimating_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimating_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimating_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("estimating_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("estimating_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("estimating_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("estimating_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("estimating_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: executive_reports', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("executive_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'executive_reports/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("executive_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("executive_reports").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'executive_reports/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("executive_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'executive_reports/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("executive_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'executive_reports/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("executive_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("executive_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("executive_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("executive_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("executive_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("executive_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("executive_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("executive_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("executive_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("executive_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("executive_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("executive_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("executive_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("executive_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("executive_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: executor_runs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("executor_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'executor_runs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("executor_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("executor_runs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'executor_runs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("executor_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'executor_runs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("executor_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'executor_runs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("executor_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("executor_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("executor_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("executor_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("executor_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("executor_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("executor_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("executor_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("executor_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("executor_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("executor_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("executor_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("executor_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("executor_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("executor_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: failed_login_attempts', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("failed_login_attempts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'failed_login_attempts/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("failed_login_attempts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("failed_login_attempts").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'failed_login_attempts/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("failed_login_attempts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'failed_login_attempts/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("failed_login_attempts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'failed_login_attempts/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("failed_login_attempts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("failed_login_attempts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("failed_login_attempts").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("failed_login_attempts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("failed_login_attempts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("failed_login_attempts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("failed_login_attempts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("failed_login_attempts").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("failed_login_attempts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("failed_login_attempts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("failed_login_attempts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("failed_login_attempts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("failed_login_attempts").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("failed_login_attempts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("failed_login_attempts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: field_captures', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("field_captures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'field_captures/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("field_captures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("field_captures").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'field_captures/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("field_captures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'field_captures/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("field_captures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'field_captures/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("field_captures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("field_captures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("field_captures").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("field_captures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("field_captures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("field_captures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("field_captures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("field_captures").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("field_captures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("field_captures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("field_captures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("field_captures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("field_captures").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("field_captures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("field_captures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: field_session_events', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("field_session_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'field_session_events/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("field_session_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("field_session_events").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'field_session_events/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("field_session_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'field_session_events/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("field_session_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'field_session_events/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("field_session_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("field_session_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("field_session_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("field_session_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("field_session_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("field_session_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("field_session_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("field_session_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("field_session_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("field_session_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("field_session_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("field_session_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("field_session_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("field_session_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("field_session_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: files', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("files").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'files/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("files").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("files").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'files/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("files").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'files/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("files").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'files/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("files").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("files").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("files").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("files").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("files").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("files").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("files").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("files").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("files").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("files").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("files").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("files").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("files").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("files").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("files").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: financial_periods', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("financial_periods").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'financial_periods/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("financial_periods").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("financial_periods").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'financial_periods/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("financial_periods").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'financial_periods/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("financial_periods").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'financial_periods/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("financial_periods").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("financial_periods").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("financial_periods").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("financial_periods").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("financial_periods").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("financial_periods").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("financial_periods").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("financial_periods").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("financial_periods").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("financial_periods").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("financial_periods").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("financial_periods").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("financial_periods").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("financial_periods").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("financial_periods").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: ifc_models', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("ifc_models").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ifc_models/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ifc_models").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ifc_models").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'ifc_models/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ifc_models").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ifc_models/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ifc_models").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'ifc_models/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("ifc_models").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ifc_models").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ifc_models").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ifc_models").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ifc_models").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("ifc_models").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ifc_models").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ifc_models").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ifc_models").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ifc_models").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("ifc_models").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("ifc_models").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("ifc_models").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("ifc_models").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("ifc_models").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: impersonation_sessions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("impersonation_sessions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'impersonation_sessions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("impersonation_sessions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("impersonation_sessions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'impersonation_sessions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("impersonation_sessions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'impersonation_sessions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("impersonation_sessions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'impersonation_sessions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("impersonation_sessions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("impersonation_sessions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("impersonation_sessions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("impersonation_sessions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("impersonation_sessions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("impersonation_sessions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("impersonation_sessions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("impersonation_sessions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("impersonation_sessions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("impersonation_sessions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("impersonation_sessions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("impersonation_sessions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("impersonation_sessions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("impersonation_sessions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("impersonation_sessions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: import_jobs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("import_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'import_jobs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("import_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("import_jobs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'import_jobs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("import_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'import_jobs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("import_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'import_jobs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("import_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("import_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("import_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("import_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("import_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("import_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("import_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("import_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("import_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("import_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("import_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("import_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("import_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("import_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("import_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: inbound_email_replies', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("inbound_email_replies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'inbound_email_replies/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inbound_email_replies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inbound_email_replies").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'inbound_email_replies/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inbound_email_replies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'inbound_email_replies/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inbound_email_replies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'inbound_email_replies/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("inbound_email_replies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inbound_email_replies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inbound_email_replies").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inbound_email_replies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inbound_email_replies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("inbound_email_replies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inbound_email_replies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inbound_email_replies").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inbound_email_replies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inbound_email_replies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("inbound_email_replies").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inbound_email_replies").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inbound_email_replies").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inbound_email_replies").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inbound_email_replies").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: inbound_email_unmatched', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("inbound_email_unmatched").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'inbound_email_unmatched/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inbound_email_unmatched").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inbound_email_unmatched").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'inbound_email_unmatched/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inbound_email_unmatched").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'inbound_email_unmatched/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inbound_email_unmatched").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'inbound_email_unmatched/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("inbound_email_unmatched").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inbound_email_unmatched").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inbound_email_unmatched").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inbound_email_unmatched").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inbound_email_unmatched").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("inbound_email_unmatched").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inbound_email_unmatched").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inbound_email_unmatched").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inbound_email_unmatched").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inbound_email_unmatched").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("inbound_email_unmatched").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inbound_email_unmatched").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inbound_email_unmatched").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inbound_email_unmatched").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inbound_email_unmatched").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: incidents', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("incidents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'incidents/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("incidents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("incidents").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'incidents/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("incidents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'incidents/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("incidents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'incidents/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("incidents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("incidents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("incidents").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("incidents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("incidents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("incidents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("incidents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("incidents").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("incidents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("incidents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("incidents").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("incidents").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("incidents").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("incidents").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("incidents").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: inspection_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("inspection_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'inspection_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inspection_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inspection_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'inspection_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inspection_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'inspection_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inspection_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'inspection_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("inspection_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inspection_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inspection_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inspection_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inspection_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("inspection_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inspection_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inspection_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inspection_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inspection_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("inspection_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("inspection_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("inspection_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("inspection_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("inspection_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: insurance_certificates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("insurance_certificates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'insurance_certificates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("insurance_certificates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("insurance_certificates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'insurance_certificates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("insurance_certificates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'insurance_certificates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("insurance_certificates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'insurance_certificates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("insurance_certificates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("insurance_certificates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("insurance_certificates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("insurance_certificates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("insurance_certificates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("insurance_certificates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("insurance_certificates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("insurance_certificates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("insurance_certificates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("insurance_certificates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("insurance_certificates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("insurance_certificates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("insurance_certificates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("insurance_certificates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("insurance_certificates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: integration_connections', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("integration_connections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_connections/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_connections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_connections").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'integration_connections/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_connections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_connections/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_connections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_connections/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("integration_connections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_connections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_connections").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_connections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_connections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("integration_connections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_connections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_connections").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_connections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_connections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("integration_connections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_connections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_connections").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_connections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_connections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: integration_field_mappings', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("integration_field_mappings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_field_mappings/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_field_mappings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_field_mappings").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'integration_field_mappings/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_field_mappings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_field_mappings/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_field_mappings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_field_mappings/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("integration_field_mappings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_field_mappings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_field_mappings").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_field_mappings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_field_mappings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("integration_field_mappings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_field_mappings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_field_mappings").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_field_mappings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_field_mappings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("integration_field_mappings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_field_mappings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_field_mappings").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_field_mappings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_field_mappings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: integration_sync_jobs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("integration_sync_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_sync_jobs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_sync_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_sync_jobs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'integration_sync_jobs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_sync_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_sync_jobs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_sync_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_sync_jobs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("integration_sync_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_sync_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_sync_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_sync_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_sync_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("integration_sync_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_sync_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_sync_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_sync_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_sync_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("integration_sync_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_sync_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_sync_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_sync_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_sync_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: integration_sync_log', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("integration_sync_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_sync_log/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_sync_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_sync_log").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'integration_sync_log/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_sync_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_sync_log/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_sync_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integration_sync_log/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("integration_sync_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_sync_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_sync_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_sync_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_sync_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("integration_sync_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_sync_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_sync_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_sync_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_sync_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("integration_sync_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integration_sync_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integration_sync_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integration_sync_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integration_sync_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: integrations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("integrations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integrations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integrations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integrations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'integrations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integrations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integrations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integrations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'integrations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("integrations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integrations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integrations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integrations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integrations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("integrations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integrations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integrations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integrations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integrations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("integrations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("integrations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("integrations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("integrations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("integrations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: invite_logs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("invite_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'invite_logs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invite_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invite_logs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'invite_logs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invite_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'invite_logs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invite_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'invite_logs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("invite_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invite_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invite_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invite_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invite_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("invite_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invite_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invite_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invite_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invite_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("invite_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invite_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invite_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invite_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invite_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: invoices', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("invoices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'invoices/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invoices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invoices").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'invoices/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invoices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'invoices/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invoices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'invoices/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("invoices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invoices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invoices").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invoices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invoices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("invoices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invoices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invoices").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invoices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invoices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("invoices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invoices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invoices").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invoices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invoices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: invoices_payable', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("invoices_payable").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'invoices_payable/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invoices_payable").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invoices_payable").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'invoices_payable/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invoices_payable").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'invoices_payable/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invoices_payable").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'invoices_payable/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("invoices_payable").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invoices_payable").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invoices_payable").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invoices_payable").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invoices_payable").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("invoices_payable").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invoices_payable").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invoices_payable").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invoices_payable").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invoices_payable").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("invoices_payable").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("invoices_payable").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("invoices_payable").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("invoices_payable").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("invoices_payable").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: iris_call_idempotency', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("iris_call_idempotency").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_call_idempotency/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_call_idempotency").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_call_idempotency").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'iris_call_idempotency/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_call_idempotency").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_call_idempotency/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_call_idempotency").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_call_idempotency/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("iris_call_idempotency").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_call_idempotency").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_call_idempotency").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_call_idempotency").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_call_idempotency").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("iris_call_idempotency").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_call_idempotency").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_call_idempotency").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_call_idempotency").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_call_idempotency").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("iris_call_idempotency").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_call_idempotency").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_call_idempotency").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_call_idempotency").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_call_idempotency").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: iris_grounding_cache', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("iris_grounding_cache").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_grounding_cache/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_grounding_cache").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_grounding_cache").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'iris_grounding_cache/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_grounding_cache").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_grounding_cache/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_grounding_cache").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_grounding_cache/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("iris_grounding_cache").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_grounding_cache").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_grounding_cache").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_grounding_cache").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_grounding_cache").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("iris_grounding_cache").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_grounding_cache").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_grounding_cache").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_grounding_cache").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_grounding_cache").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("iris_grounding_cache").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_grounding_cache").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_grounding_cache").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_grounding_cache").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_grounding_cache").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: iris_kb_chunks', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("iris_kb_chunks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_kb_chunks/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_chunks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_chunks").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'iris_kb_chunks/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_chunks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_kb_chunks/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_chunks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_kb_chunks/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("iris_kb_chunks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_chunks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_chunks").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_chunks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_chunks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("iris_kb_chunks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_chunks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_chunks").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_chunks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_chunks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("iris_kb_chunks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_chunks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_chunks").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_chunks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_chunks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: iris_kb_sources', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("iris_kb_sources").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_kb_sources/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_sources").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_sources").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'iris_kb_sources/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_sources").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_kb_sources/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_sources").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_kb_sources/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("iris_kb_sources").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_sources").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_sources").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_sources").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_sources").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("iris_kb_sources").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_sources").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_sources").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_sources").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_sources").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("iris_kb_sources").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_sources").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_sources").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_sources").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_sources").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: iris_kb_telemetry', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("iris_kb_telemetry").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_kb_telemetry/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_telemetry").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_telemetry").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'iris_kb_telemetry/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_telemetry").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_kb_telemetry/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_telemetry").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_kb_telemetry/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("iris_kb_telemetry").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_telemetry").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_telemetry").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_telemetry").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_telemetry").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("iris_kb_telemetry").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_telemetry").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_telemetry").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_telemetry").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_telemetry").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("iris_kb_telemetry").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_kb_telemetry").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_kb_telemetry").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_kb_telemetry").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_kb_telemetry").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: iris_personas', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("iris_personas").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_personas/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_personas").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_personas").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'iris_personas/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_personas").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_personas/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_personas").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_personas/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("iris_personas").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_personas").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_personas").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_personas").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_personas").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("iris_personas").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_personas").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_personas").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_personas").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_personas").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("iris_personas").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_personas").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_personas").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_personas").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_personas").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: iris_suggestion_history', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("iris_suggestion_history").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_suggestion_history/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_suggestion_history").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_suggestion_history").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'iris_suggestion_history/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_suggestion_history").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_suggestion_history/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_suggestion_history").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_suggestion_history/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("iris_suggestion_history").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_suggestion_history").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_suggestion_history").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_suggestion_history").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_suggestion_history").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("iris_suggestion_history").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_suggestion_history").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_suggestion_history").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_suggestion_history").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_suggestion_history").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("iris_suggestion_history").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_suggestion_history").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_suggestion_history").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_suggestion_history").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_suggestion_history").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: iris_telemetry', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("iris_telemetry").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_telemetry/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_telemetry").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_telemetry").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'iris_telemetry/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_telemetry").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_telemetry/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_telemetry").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_telemetry/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("iris_telemetry").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_telemetry").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_telemetry").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_telemetry").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_telemetry").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("iris_telemetry").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_telemetry").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_telemetry").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_telemetry").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_telemetry").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("iris_telemetry").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_telemetry").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_telemetry").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_telemetry").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_telemetry").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: iris_user_personas', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("iris_user_personas").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_user_personas/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_user_personas").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_user_personas").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'iris_user_personas/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_user_personas").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_user_personas/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_user_personas").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_user_personas/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("iris_user_personas").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_user_personas").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_user_personas").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_user_personas").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_user_personas").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("iris_user_personas").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_user_personas").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_user_personas").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_user_personas").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_user_personas").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("iris_user_personas").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_user_personas").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_user_personas").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_user_personas").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_user_personas").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: iris_voice_diffs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("iris_voice_diffs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_voice_diffs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_voice_diffs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_voice_diffs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'iris_voice_diffs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_voice_diffs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_voice_diffs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_voice_diffs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_voice_diffs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("iris_voice_diffs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_voice_diffs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_voice_diffs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_voice_diffs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_voice_diffs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("iris_voice_diffs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_voice_diffs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_voice_diffs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_voice_diffs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_voice_diffs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("iris_voice_diffs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_voice_diffs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_voice_diffs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_voice_diffs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_voice_diffs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: iris_weekly_digests', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("iris_weekly_digests").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_weekly_digests/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_weekly_digests").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_weekly_digests").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'iris_weekly_digests/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_weekly_digests").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_weekly_digests/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_weekly_digests").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'iris_weekly_digests/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("iris_weekly_digests").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_weekly_digests").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_weekly_digests").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_weekly_digests").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_weekly_digests").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("iris_weekly_digests").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_weekly_digests").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_weekly_digests").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_weekly_digests").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_weekly_digests").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("iris_weekly_digests").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("iris_weekly_digests").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("iris_weekly_digests").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("iris_weekly_digests").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("iris_weekly_digests").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: job_cost_entries', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("job_cost_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'job_cost_entries/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("job_cost_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("job_cost_entries").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'job_cost_entries/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("job_cost_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'job_cost_entries/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("job_cost_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'job_cost_entries/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("job_cost_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("job_cost_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("job_cost_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("job_cost_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("job_cost_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("job_cost_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("job_cost_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("job_cost_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("job_cost_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("job_cost_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("job_cost_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("job_cost_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("job_cost_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("job_cost_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("job_cost_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: labor_forecasts', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("labor_forecasts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'labor_forecasts/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("labor_forecasts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("labor_forecasts").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'labor_forecasts/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("labor_forecasts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'labor_forecasts/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("labor_forecasts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'labor_forecasts/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("labor_forecasts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("labor_forecasts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("labor_forecasts").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("labor_forecasts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("labor_forecasts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("labor_forecasts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("labor_forecasts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("labor_forecasts").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("labor_forecasts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("labor_forecasts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("labor_forecasts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("labor_forecasts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("labor_forecasts").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("labor_forecasts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("labor_forecasts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: labor_rates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("labor_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'labor_rates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("labor_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("labor_rates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'labor_rates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("labor_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'labor_rates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("labor_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'labor_rates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("labor_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("labor_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("labor_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("labor_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("labor_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("labor_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("labor_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("labor_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("labor_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("labor_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("labor_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("labor_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("labor_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("labor_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("labor_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: leed_credits', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("leed_credits").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'leed_credits/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("leed_credits").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("leed_credits").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'leed_credits/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("leed_credits").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'leed_credits/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("leed_credits").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'leed_credits/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("leed_credits").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("leed_credits").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("leed_credits").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("leed_credits").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("leed_credits").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("leed_credits").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("leed_credits").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("leed_credits").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("leed_credits").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("leed_credits").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("leed_credits").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("leed_credits").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("leed_credits").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("leed_credits").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("leed_credits").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: lien_waiver_signatures', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("lien_waiver_signatures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'lien_waiver_signatures/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("lien_waiver_signatures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("lien_waiver_signatures").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'lien_waiver_signatures/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("lien_waiver_signatures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'lien_waiver_signatures/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("lien_waiver_signatures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'lien_waiver_signatures/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("lien_waiver_signatures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("lien_waiver_signatures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("lien_waiver_signatures").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("lien_waiver_signatures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("lien_waiver_signatures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("lien_waiver_signatures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("lien_waiver_signatures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("lien_waiver_signatures").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("lien_waiver_signatures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("lien_waiver_signatures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("lien_waiver_signatures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("lien_waiver_signatures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("lien_waiver_signatures").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("lien_waiver_signatures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("lien_waiver_signatures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: lien_waivers', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("lien_waivers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'lien_waivers/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("lien_waivers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("lien_waivers").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'lien_waivers/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("lien_waivers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'lien_waivers/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("lien_waivers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'lien_waivers/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("lien_waivers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("lien_waivers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("lien_waivers").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("lien_waivers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("lien_waivers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("lien_waivers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("lien_waivers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("lien_waivers").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("lien_waivers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("lien_waivers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("lien_waivers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("lien_waivers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("lien_waivers").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("lien_waivers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("lien_waivers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: magic_link_tokens', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("magic_link_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'magic_link_tokens/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("magic_link_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("magic_link_tokens").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'magic_link_tokens/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("magic_link_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'magic_link_tokens/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("magic_link_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'magic_link_tokens/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("magic_link_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("magic_link_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("magic_link_tokens").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("magic_link_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("magic_link_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("magic_link_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("magic_link_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("magic_link_tokens").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("magic_link_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("magic_link_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("magic_link_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("magic_link_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("magic_link_tokens").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("magic_link_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("magic_link_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: material_inventory', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("material_inventory").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'material_inventory/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_inventory").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_inventory").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'material_inventory/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_inventory").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'material_inventory/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_inventory").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'material_inventory/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("material_inventory").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_inventory").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_inventory").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_inventory").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_inventory").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("material_inventory").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_inventory").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_inventory").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_inventory").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_inventory").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("material_inventory").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_inventory").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_inventory").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_inventory").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_inventory").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: material_prices', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("material_prices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'material_prices/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_prices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_prices").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'material_prices/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_prices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'material_prices/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_prices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'material_prices/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("material_prices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_prices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_prices").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_prices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_prices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("material_prices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_prices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_prices").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_prices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_prices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("material_prices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_prices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_prices").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_prices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_prices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: material_rates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("material_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'material_rates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_rates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'material_rates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'material_rates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'material_rates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("material_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("material_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("material_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("material_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("material_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("material_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("material_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: media_links', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("media_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'media_links/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("media_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("media_links").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'media_links/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("media_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'media_links/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("media_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'media_links/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("media_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("media_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("media_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("media_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("media_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("media_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("media_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("media_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("media_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("media_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("media_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("media_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("media_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("media_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("media_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: meeting_action_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("meeting_action_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_action_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_action_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_action_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'meeting_action_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_action_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_action_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_action_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_action_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("meeting_action_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_action_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_action_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_action_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_action_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("meeting_action_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_action_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_action_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_action_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_action_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("meeting_action_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_action_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_action_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_action_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_action_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: meeting_agenda_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("meeting_agenda_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_agenda_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_agenda_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_agenda_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'meeting_agenda_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_agenda_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_agenda_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_agenda_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_agenda_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("meeting_agenda_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_agenda_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_agenda_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_agenda_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_agenda_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("meeting_agenda_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_agenda_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_agenda_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_agenda_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_agenda_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("meeting_agenda_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_agenda_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_agenda_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_agenda_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_agenda_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: meeting_attendees', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("meeting_attendees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_attendees/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_attendees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_attendees").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'meeting_attendees/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_attendees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_attendees/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_attendees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_attendees/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("meeting_attendees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_attendees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_attendees").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_attendees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_attendees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("meeting_attendees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_attendees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_attendees").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_attendees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_attendees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("meeting_attendees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_attendees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_attendees").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_attendees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_attendees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: meeting_participants', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("meeting_participants").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_participants/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_participants").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_participants").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'meeting_participants/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_participants").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_participants/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_participants").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_participants/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("meeting_participants").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_participants").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_participants").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_participants").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_participants").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("meeting_participants").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_participants").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_participants").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_participants").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_participants").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("meeting_participants").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_participants").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_participants").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_participants").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_participants").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: meeting_series', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("meeting_series").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_series/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_series").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_series").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'meeting_series/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_series").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_series/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_series").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meeting_series/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("meeting_series").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_series").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_series").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_series").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_series").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("meeting_series").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_series").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_series").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_series").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_series").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("meeting_series").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meeting_series").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meeting_series").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meeting_series").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meeting_series").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: meetings', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("meetings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meetings/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meetings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meetings").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'meetings/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meetings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meetings/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meetings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'meetings/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("meetings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meetings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meetings").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meetings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meetings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("meetings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meetings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meetings").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meetings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meetings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("meetings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("meetings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("meetings").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("meetings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("meetings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: message_reactions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("message_reactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'message_reactions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("message_reactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("message_reactions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'message_reactions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("message_reactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'message_reactions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("message_reactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'message_reactions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("message_reactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("message_reactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("message_reactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("message_reactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("message_reactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("message_reactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("message_reactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("message_reactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("message_reactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("message_reactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("message_reactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("message_reactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("message_reactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("message_reactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("message_reactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: notification_preferences', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("notification_preferences").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'notification_preferences/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notification_preferences").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notification_preferences").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'notification_preferences/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notification_preferences").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'notification_preferences/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notification_preferences").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'notification_preferences/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("notification_preferences").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notification_preferences").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notification_preferences").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notification_preferences").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notification_preferences").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("notification_preferences").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notification_preferences").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notification_preferences").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notification_preferences").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notification_preferences").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("notification_preferences").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notification_preferences").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notification_preferences").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notification_preferences").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notification_preferences").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: notification_queue', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("notification_queue").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'notification_queue/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notification_queue").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notification_queue").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'notification_queue/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notification_queue").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'notification_queue/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notification_queue").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'notification_queue/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("notification_queue").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notification_queue").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notification_queue").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notification_queue").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notification_queue").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("notification_queue").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notification_queue").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notification_queue").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notification_queue").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notification_queue").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("notification_queue").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notification_queue").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notification_queue").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notification_queue").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notification_queue").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: notifications', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("notifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'notifications/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notifications").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'notifications/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'notifications/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'notifications/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("notifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("notifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("notifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("notifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("notifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("notifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("notifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: org_api_token_uses', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("org_api_token_uses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_api_token_uses/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_api_token_uses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_api_token_uses").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'org_api_token_uses/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_api_token_uses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_api_token_uses/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_api_token_uses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_api_token_uses/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("org_api_token_uses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_api_token_uses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_api_token_uses").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_api_token_uses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_api_token_uses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("org_api_token_uses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_api_token_uses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_api_token_uses").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_api_token_uses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_api_token_uses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("org_api_token_uses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_api_token_uses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_api_token_uses").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_api_token_uses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_api_token_uses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: org_api_tokens', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("org_api_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_api_tokens/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_api_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_api_tokens").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'org_api_tokens/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_api_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_api_tokens/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_api_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_api_tokens/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("org_api_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_api_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_api_tokens").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_api_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_api_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("org_api_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_api_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_api_tokens").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_api_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_api_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("org_api_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_api_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_api_tokens").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_api_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_api_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: org_branding', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("org_branding").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_branding/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_branding").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_branding").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'org_branding/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_branding").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_branding/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_branding").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_branding/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("org_branding").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_branding").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_branding").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_branding").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_branding").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("org_branding").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_branding").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_branding").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_branding").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_branding").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("org_branding").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_branding").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_branding").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_branding").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_branding").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: org_custom_role_assignments', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("org_custom_role_assignments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_custom_role_assignments/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_custom_role_assignments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_custom_role_assignments").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'org_custom_role_assignments/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_custom_role_assignments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_custom_role_assignments/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_custom_role_assignments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_custom_role_assignments/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("org_custom_role_assignments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_custom_role_assignments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_custom_role_assignments").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_custom_role_assignments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_custom_role_assignments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("org_custom_role_assignments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_custom_role_assignments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_custom_role_assignments").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_custom_role_assignments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_custom_role_assignments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("org_custom_role_assignments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_custom_role_assignments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_custom_role_assignments").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_custom_role_assignments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_custom_role_assignments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: org_custom_roles', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("org_custom_roles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_custom_roles/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_custom_roles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_custom_roles").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'org_custom_roles/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_custom_roles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_custom_roles/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_custom_roles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_custom_roles/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("org_custom_roles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_custom_roles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_custom_roles").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_custom_roles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_custom_roles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("org_custom_roles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_custom_roles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_custom_roles").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_custom_roles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_custom_roles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("org_custom_roles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_custom_roles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_custom_roles").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_custom_roles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_custom_roles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: org_s3_export_config', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("org_s3_export_config").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_s3_export_config/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_s3_export_config").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_s3_export_config").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'org_s3_export_config/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_s3_export_config").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_s3_export_config/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_s3_export_config").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_s3_export_config/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("org_s3_export_config").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_s3_export_config").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_s3_export_config").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_s3_export_config").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_s3_export_config").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("org_s3_export_config").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_s3_export_config").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_s3_export_config").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_s3_export_config").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_s3_export_config").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("org_s3_export_config").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_s3_export_config").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_s3_export_config").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_s3_export_config").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_s3_export_config").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: org_sso_config', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("org_sso_config").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_sso_config/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_sso_config").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_sso_config").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'org_sso_config/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_sso_config").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_sso_config/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_sso_config").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'org_sso_config/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("org_sso_config").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_sso_config").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_sso_config").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_sso_config").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_sso_config").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("org_sso_config").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_sso_config").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_sso_config").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_sso_config").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_sso_config").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("org_sso_config").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("org_sso_config").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("org_sso_config").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("org_sso_config").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("org_sso_config").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: organism_cycles', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("organism_cycles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_cycles/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_cycles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_cycles").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'organism_cycles/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_cycles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_cycles/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_cycles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_cycles/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("organism_cycles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_cycles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_cycles").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_cycles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_cycles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("organism_cycles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_cycles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_cycles").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_cycles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_cycles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("organism_cycles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_cycles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_cycles").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_cycles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_cycles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: organism_experiments', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("organism_experiments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_experiments/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_experiments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_experiments").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'organism_experiments/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_experiments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_experiments/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_experiments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_experiments/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("organism_experiments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_experiments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_experiments").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_experiments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_experiments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("organism_experiments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_experiments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_experiments").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_experiments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_experiments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("organism_experiments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_experiments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_experiments").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_experiments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_experiments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: organism_learnings', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("organism_learnings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_learnings/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_learnings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_learnings").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'organism_learnings/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_learnings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_learnings/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_learnings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_learnings/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("organism_learnings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_learnings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_learnings").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_learnings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_learnings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("organism_learnings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_learnings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_learnings").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_learnings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_learnings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("organism_learnings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_learnings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_learnings").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_learnings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_learnings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: organism_skills', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("organism_skills").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_skills/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_skills").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_skills").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'organism_skills/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_skills").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_skills/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_skills").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organism_skills/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("organism_skills").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_skills").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_skills").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_skills").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_skills").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("organism_skills").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_skills").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_skills").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_skills").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_skills").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("organism_skills").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organism_skills").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organism_skills").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organism_skills").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organism_skills").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: organization_members', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("organization_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organization_members/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organization_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organization_members").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'organization_members/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organization_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organization_members/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organization_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organization_members/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("organization_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organization_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organization_members").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organization_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organization_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("organization_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organization_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organization_members").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organization_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organization_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("organization_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organization_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organization_members").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organization_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organization_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: organization_settings', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("organization_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organization_settings/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organization_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organization_settings").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'organization_settings/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organization_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organization_settings/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organization_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organization_settings/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("organization_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organization_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organization_settings").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organization_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organization_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("organization_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organization_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organization_settings").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organization_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organization_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("organization_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organization_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organization_settings").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organization_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organization_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: organizations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("organizations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organizations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organizations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organizations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'organizations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organizations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organizations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organizations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'organizations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("organizations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organizations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organizations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organizations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organizations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("organizations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organizations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organizations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organizations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organizations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("organizations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("organizations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("organizations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("organizations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("organizations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: outbound_email_log', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("outbound_email_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'outbound_email_log/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("outbound_email_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("outbound_email_log").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'outbound_email_log/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("outbound_email_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'outbound_email_log/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("outbound_email_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'outbound_email_log/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("outbound_email_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("outbound_email_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("outbound_email_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("outbound_email_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("outbound_email_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("outbound_email_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("outbound_email_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("outbound_email_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("outbound_email_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("outbound_email_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("outbound_email_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("outbound_email_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("outbound_email_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("outbound_email_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("outbound_email_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: outbound_webhooks', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("outbound_webhooks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'outbound_webhooks/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("outbound_webhooks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("outbound_webhooks").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'outbound_webhooks/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("outbound_webhooks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'outbound_webhooks/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("outbound_webhooks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'outbound_webhooks/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("outbound_webhooks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("outbound_webhooks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("outbound_webhooks").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("outbound_webhooks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("outbound_webhooks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("outbound_webhooks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("outbound_webhooks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("outbound_webhooks").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("outbound_webhooks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("outbound_webhooks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("outbound_webhooks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("outbound_webhooks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("outbound_webhooks").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("outbound_webhooks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("outbound_webhooks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: owner_updates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("owner_updates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'owner_updates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("owner_updates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("owner_updates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'owner_updates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("owner_updates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'owner_updates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("owner_updates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'owner_updates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("owner_updates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("owner_updates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("owner_updates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("owner_updates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("owner_updates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("owner_updates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("owner_updates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("owner_updates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("owner_updates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("owner_updates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("owner_updates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("owner_updates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("owner_updates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("owner_updates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("owner_updates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: pay_app_reconciliation_lines', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("pay_app_reconciliation_lines").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_app_reconciliation_lines/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_app_reconciliation_lines").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_app_reconciliation_lines").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'pay_app_reconciliation_lines/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_app_reconciliation_lines").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_app_reconciliation_lines/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_app_reconciliation_lines").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_app_reconciliation_lines/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("pay_app_reconciliation_lines").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_app_reconciliation_lines").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_app_reconciliation_lines").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_app_reconciliation_lines").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_app_reconciliation_lines").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("pay_app_reconciliation_lines").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_app_reconciliation_lines").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_app_reconciliation_lines").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_app_reconciliation_lines").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_app_reconciliation_lines").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("pay_app_reconciliation_lines").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_app_reconciliation_lines").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_app_reconciliation_lines").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_app_reconciliation_lines").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_app_reconciliation_lines").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: pay_app_reconciliations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("pay_app_reconciliations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_app_reconciliations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_app_reconciliations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_app_reconciliations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'pay_app_reconciliations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_app_reconciliations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_app_reconciliations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_app_reconciliations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_app_reconciliations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("pay_app_reconciliations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_app_reconciliations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_app_reconciliations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_app_reconciliations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_app_reconciliations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("pay_app_reconciliations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_app_reconciliations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_app_reconciliations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_app_reconciliations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_app_reconciliations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("pay_app_reconciliations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_app_reconciliations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_app_reconciliations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_app_reconciliations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_app_reconciliations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: pay_application_line_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("pay_application_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_application_line_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_application_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_application_line_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'pay_application_line_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_application_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_application_line_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_application_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_application_line_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("pay_application_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_application_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_application_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_application_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_application_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("pay_application_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_application_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_application_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_application_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_application_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("pay_application_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_application_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_application_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_application_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_application_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: pay_applications', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("pay_applications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_applications/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_applications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_applications").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'pay_applications/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_applications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_applications/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_applications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pay_applications/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("pay_applications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_applications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_applications").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_applications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_applications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("pay_applications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_applications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_applications").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_applications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_applications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("pay_applications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pay_applications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pay_applications").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pay_applications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pay_applications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: payapp_audit_overrides', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("payapp_audit_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_audit_overrides/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_audit_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_audit_overrides").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'payapp_audit_overrides/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_audit_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_audit_overrides/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_audit_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_audit_overrides/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("payapp_audit_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_audit_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_audit_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_audit_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_audit_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("payapp_audit_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_audit_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_audit_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_audit_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_audit_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("payapp_audit_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_audit_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_audit_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_audit_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_audit_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: payapp_audit_runs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("payapp_audit_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_audit_runs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_audit_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_audit_runs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'payapp_audit_runs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_audit_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_audit_runs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_audit_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_audit_runs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("payapp_audit_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_audit_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_audit_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_audit_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_audit_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("payapp_audit_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_audit_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_audit_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_audit_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_audit_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("payapp_audit_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_audit_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_audit_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_audit_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_audit_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: payapp_owner_preview_comments', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("payapp_owner_preview_comments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_owner_preview_comments/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_owner_preview_comments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_owner_preview_comments").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'payapp_owner_preview_comments/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_owner_preview_comments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_owner_preview_comments/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_owner_preview_comments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_owner_preview_comments/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("payapp_owner_preview_comments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_owner_preview_comments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_owner_preview_comments").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_owner_preview_comments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_owner_preview_comments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("payapp_owner_preview_comments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_owner_preview_comments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_owner_preview_comments").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_owner_preview_comments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_owner_preview_comments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("payapp_owner_preview_comments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_owner_preview_comments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_owner_preview_comments").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_owner_preview_comments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_owner_preview_comments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: payapp_owner_previews', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("payapp_owner_previews").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_owner_previews/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_owner_previews").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_owner_previews").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'payapp_owner_previews/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_owner_previews").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_owner_previews/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_owner_previews").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payapp_owner_previews/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("payapp_owner_previews").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_owner_previews").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_owner_previews").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_owner_previews").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_owner_previews").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("payapp_owner_previews").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_owner_previews").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_owner_previews").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_owner_previews").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_owner_previews").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("payapp_owner_previews").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payapp_owner_previews").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payapp_owner_previews").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payapp_owner_previews").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payapp_owner_previews").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: payment_applications', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("payment_applications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_applications/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_applications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_applications").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'payment_applications/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_applications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_applications/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_applications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_applications/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("payment_applications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_applications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_applications").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_applications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_applications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("payment_applications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_applications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_applications").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_applications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_applications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("payment_applications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_applications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_applications").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_applications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_applications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: payment_line_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("payment_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_line_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_line_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'payment_line_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_line_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_line_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("payment_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("payment_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("payment_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: payment_methods', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("payment_methods").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_methods/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_methods").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_methods").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'payment_methods/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_methods").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_methods/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_methods").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_methods/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("payment_methods").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_methods").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_methods").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_methods").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_methods").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("payment_methods").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_methods").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_methods").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_methods").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_methods").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("payment_methods").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_methods").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_methods").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_methods").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_methods").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: payment_milestones', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("payment_milestones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_milestones/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_milestones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_milestones").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'payment_milestones/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_milestones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_milestones/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_milestones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_milestones/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("payment_milestones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_milestones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_milestones").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_milestones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_milestones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("payment_milestones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_milestones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_milestones").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_milestones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_milestones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("payment_milestones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_milestones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_milestones").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_milestones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_milestones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: payment_transactions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("payment_transactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_transactions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_transactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_transactions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'payment_transactions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_transactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_transactions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_transactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'payment_transactions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("payment_transactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_transactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_transactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_transactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_transactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("payment_transactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_transactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_transactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_transactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_transactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("payment_transactions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("payment_transactions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("payment_transactions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("payment_transactions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("payment_transactions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: per_project_role_overrides', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("per_project_role_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'per_project_role_overrides/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("per_project_role_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("per_project_role_overrides").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'per_project_role_overrides/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("per_project_role_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'per_project_role_overrides/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("per_project_role_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'per_project_role_overrides/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("per_project_role_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("per_project_role_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("per_project_role_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("per_project_role_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("per_project_role_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("per_project_role_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("per_project_role_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("per_project_role_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("per_project_role_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("per_project_role_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("per_project_role_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("per_project_role_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("per_project_role_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("per_project_role_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("per_project_role_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: permit_inspections', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("permit_inspections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'permit_inspections/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("permit_inspections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("permit_inspections").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'permit_inspections/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("permit_inspections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'permit_inspections/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("permit_inspections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'permit_inspections/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("permit_inspections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("permit_inspections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("permit_inspections").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("permit_inspections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("permit_inspections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("permit_inspections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("permit_inspections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("permit_inspections").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("permit_inspections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("permit_inspections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("permit_inspections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("permit_inspections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("permit_inspections").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("permit_inspections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("permit_inspections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: permits', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("permits").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'permits/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("permits").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("permits").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'permits/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("permits").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'permits/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("permits").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'permits/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("permits").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("permits").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("permits").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("permits").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("permits").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("permits").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("permits").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("permits").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("permits").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("permits").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("permits").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("permits").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("permits").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("permits").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("permits").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: photo_comparisons', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("photo_comparisons").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'photo_comparisons/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_comparisons").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_comparisons").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'photo_comparisons/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_comparisons").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'photo_comparisons/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_comparisons").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'photo_comparisons/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("photo_comparisons").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_comparisons").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_comparisons").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_comparisons").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_comparisons").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("photo_comparisons").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_comparisons").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_comparisons").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_comparisons").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_comparisons").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("photo_comparisons").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_comparisons").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_comparisons").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_comparisons").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_comparisons").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: photo_pin_associations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("photo_pin_associations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'photo_pin_associations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_pin_associations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_pin_associations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'photo_pin_associations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_pin_associations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'photo_pin_associations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_pin_associations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'photo_pin_associations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("photo_pin_associations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_pin_associations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_pin_associations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_pin_associations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_pin_associations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("photo_pin_associations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_pin_associations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_pin_associations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_pin_associations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_pin_associations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("photo_pin_associations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_pin_associations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_pin_associations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_pin_associations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_pin_associations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: photo_pins', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("photo_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'photo_pins/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_pins").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'photo_pins/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'photo_pins/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'photo_pins/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("photo_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("photo_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("photo_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("photo_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("photo_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("photo_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("photo_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: pilot_agreements', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("pilot_agreements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pilot_agreements/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pilot_agreements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pilot_agreements").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'pilot_agreements/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pilot_agreements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pilot_agreements/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pilot_agreements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pilot_agreements/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("pilot_agreements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pilot_agreements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pilot_agreements").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pilot_agreements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pilot_agreements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("pilot_agreements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pilot_agreements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pilot_agreements").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pilot_agreements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pilot_agreements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("pilot_agreements").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pilot_agreements").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pilot_agreements").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pilot_agreements").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pilot_agreements").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: plans', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'plans/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("plans").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'plans/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'plans/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'plans/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("plans").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("plans").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("plans").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: po_line_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("po_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'po_line_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("po_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("po_line_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'po_line_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("po_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'po_line_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("po_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'po_line_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("po_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("po_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("po_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("po_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("po_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("po_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("po_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("po_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("po_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("po_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("po_line_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("po_line_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("po_line_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("po_line_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("po_line_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: portal_access_tokens', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("portal_access_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portal_access_tokens/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_access_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_access_tokens").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'portal_access_tokens/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_access_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portal_access_tokens/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_access_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portal_access_tokens/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("portal_access_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_access_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_access_tokens").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_access_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_access_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("portal_access_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_access_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_access_tokens").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_access_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_access_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("portal_access_tokens").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_access_tokens").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_access_tokens").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_access_tokens").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_access_tokens").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: portal_invitations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("portal_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portal_invitations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_invitations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'portal_invitations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portal_invitations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portal_invitations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("portal_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("portal_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("portal_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: portal_users', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("portal_users").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portal_users/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_users").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_users").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'portal_users/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_users").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portal_users/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_users").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portal_users/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("portal_users").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_users").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_users").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_users").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_users").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("portal_users").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_users").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_users").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_users").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_users").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("portal_users").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portal_users").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portal_users").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portal_users").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portal_users").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: portfolio_projects', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("portfolio_projects").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portfolio_projects/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portfolio_projects").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portfolio_projects").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'portfolio_projects/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portfolio_projects").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portfolio_projects/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portfolio_projects").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portfolio_projects/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("portfolio_projects").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portfolio_projects").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portfolio_projects").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portfolio_projects").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portfolio_projects").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("portfolio_projects").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portfolio_projects").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portfolio_projects").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portfolio_projects").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portfolio_projects").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("portfolio_projects").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portfolio_projects").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portfolio_projects").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portfolio_projects").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portfolio_projects").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: portfolios', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("portfolios").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portfolios/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portfolios").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portfolios").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'portfolios/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portfolios").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portfolios/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portfolios").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'portfolios/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("portfolios").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portfolios").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portfolios").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portfolios").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portfolios").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("portfolios").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portfolios").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portfolios").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portfolios").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portfolios").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("portfolios").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("portfolios").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("portfolios").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("portfolios").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("portfolios").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: pre_task_plans', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("pre_task_plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pre_task_plans/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pre_task_plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pre_task_plans").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'pre_task_plans/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pre_task_plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pre_task_plans/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pre_task_plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'pre_task_plans/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("pre_task_plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pre_task_plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pre_task_plans").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pre_task_plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pre_task_plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("pre_task_plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pre_task_plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pre_task_plans").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pre_task_plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pre_task_plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("pre_task_plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("pre_task_plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("pre_task_plans").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("pre_task_plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("pre_task_plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: precon_bid_invitations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("precon_bid_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_invitations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_invitations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'precon_bid_invitations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_invitations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_invitations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("precon_bid_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("precon_bid_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("precon_bid_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: precon_bid_packages', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("precon_bid_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_packages/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_packages").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'precon_bid_packages/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_packages/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_packages/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("precon_bid_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_packages").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("precon_bid_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_packages").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("precon_bid_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_packages").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: precon_bid_scope_responses', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("precon_bid_scope_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_scope_responses/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_scope_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_scope_responses").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'precon_bid_scope_responses/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_scope_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_scope_responses/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_scope_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_scope_responses/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("precon_bid_scope_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_scope_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_scope_responses").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_scope_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_scope_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("precon_bid_scope_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_scope_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_scope_responses").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_scope_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_scope_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("precon_bid_scope_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_scope_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_scope_responses").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_scope_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_scope_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: precon_bid_submissions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("precon_bid_submissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_submissions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_submissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_submissions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'precon_bid_submissions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_submissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_submissions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_submissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_bid_submissions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("precon_bid_submissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_submissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_submissions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_submissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_submissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("precon_bid_submissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_submissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_submissions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_submissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_submissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("precon_bid_submissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_bid_submissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_bid_submissions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_bid_submissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_bid_submissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: precon_scope_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("precon_scope_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_scope_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_scope_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_scope_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'precon_scope_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_scope_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_scope_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_scope_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_scope_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("precon_scope_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_scope_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_scope_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_scope_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_scope_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("precon_scope_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_scope_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_scope_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_scope_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_scope_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("precon_scope_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_scope_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_scope_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_scope_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_scope_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: precon_subcontractors', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("precon_subcontractors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_subcontractors/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_subcontractors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_subcontractors").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'precon_subcontractors/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_subcontractors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_subcontractors/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_subcontractors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'precon_subcontractors/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("precon_subcontractors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_subcontractors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_subcontractors").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_subcontractors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_subcontractors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("precon_subcontractors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_subcontractors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_subcontractors").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_subcontractors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_subcontractors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("precon_subcontractors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("precon_subcontractors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("precon_subcontractors").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("precon_subcontractors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("precon_subcontractors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: prequalifications', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("prequalifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'prequalifications/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prequalifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prequalifications").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'prequalifications/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prequalifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'prequalifications/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prequalifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'prequalifications/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("prequalifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prequalifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prequalifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prequalifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prequalifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("prequalifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prequalifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prequalifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prequalifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prequalifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("prequalifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prequalifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prequalifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prequalifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prequalifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: presence_heartbeats', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("presence_heartbeats").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'presence_heartbeats/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("presence_heartbeats").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("presence_heartbeats").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'presence_heartbeats/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("presence_heartbeats").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'presence_heartbeats/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("presence_heartbeats").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'presence_heartbeats/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("presence_heartbeats").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("presence_heartbeats").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("presence_heartbeats").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("presence_heartbeats").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("presence_heartbeats").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("presence_heartbeats").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("presence_heartbeats").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("presence_heartbeats").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("presence_heartbeats").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("presence_heartbeats").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("presence_heartbeats").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("presence_heartbeats").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("presence_heartbeats").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("presence_heartbeats").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("presence_heartbeats").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: presence_room_keys', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("presence_room_keys").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'presence_room_keys/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("presence_room_keys").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("presence_room_keys").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'presence_room_keys/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("presence_room_keys").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'presence_room_keys/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("presence_room_keys").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'presence_room_keys/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("presence_room_keys").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("presence_room_keys").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("presence_room_keys").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("presence_room_keys").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("presence_room_keys").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("presence_room_keys").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("presence_room_keys").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("presence_room_keys").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("presence_room_keys").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("presence_room_keys").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("presence_room_keys").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("presence_room_keys").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("presence_room_keys").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("presence_room_keys").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("presence_room_keys").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: prevailing_wage_decisions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("prevailing_wage_decisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'prevailing_wage_decisions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prevailing_wage_decisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prevailing_wage_decisions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'prevailing_wage_decisions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prevailing_wage_decisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'prevailing_wage_decisions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prevailing_wage_decisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'prevailing_wage_decisions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("prevailing_wage_decisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prevailing_wage_decisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prevailing_wage_decisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prevailing_wage_decisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prevailing_wage_decisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("prevailing_wage_decisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prevailing_wage_decisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prevailing_wage_decisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prevailing_wage_decisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prevailing_wage_decisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("prevailing_wage_decisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prevailing_wage_decisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prevailing_wage_decisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prevailing_wage_decisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prevailing_wage_decisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: prevailing_wage_rates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("prevailing_wage_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'prevailing_wage_rates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prevailing_wage_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prevailing_wage_rates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'prevailing_wage_rates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prevailing_wage_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'prevailing_wage_rates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prevailing_wage_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'prevailing_wage_rates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("prevailing_wage_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prevailing_wage_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prevailing_wage_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prevailing_wage_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prevailing_wage_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("prevailing_wage_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prevailing_wage_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prevailing_wage_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prevailing_wage_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prevailing_wage_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("prevailing_wage_rates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("prevailing_wage_rates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("prevailing_wage_rates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("prevailing_wage_rates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("prevailing_wage_rates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: profiles', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("profiles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'profiles/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("profiles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("profiles").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'profiles/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("profiles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'profiles/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("profiles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'profiles/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("profiles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("profiles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("profiles").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("profiles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("profiles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("profiles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("profiles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("profiles").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("profiles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("profiles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("profiles").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("profiles").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("profiles").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("profiles").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("profiles").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: progress_detection_results', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("progress_detection_results").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'progress_detection_results/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("progress_detection_results").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("progress_detection_results").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'progress_detection_results/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("progress_detection_results").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'progress_detection_results/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("progress_detection_results").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'progress_detection_results/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("progress_detection_results").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("progress_detection_results").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("progress_detection_results").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("progress_detection_results").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("progress_detection_results").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("progress_detection_results").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("progress_detection_results").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("progress_detection_results").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("progress_detection_results").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("progress_detection_results").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("progress_detection_results").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("progress_detection_results").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("progress_detection_results").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("progress_detection_results").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("progress_detection_results").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_business_calendar', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_business_calendar").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_business_calendar/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_business_calendar").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_business_calendar").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_business_calendar/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_business_calendar").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_business_calendar/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_business_calendar").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_business_calendar/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_business_calendar").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_business_calendar").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_business_calendar").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_business_calendar").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_business_calendar").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_business_calendar").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_business_calendar").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_business_calendar").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_business_calendar").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_business_calendar").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_business_calendar").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_business_calendar").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_business_calendar").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_business_calendar").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_business_calendar").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_carbon_entries', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_carbon_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_carbon_entries/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_carbon_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_carbon_entries").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_carbon_entries/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_carbon_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_carbon_entries/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_carbon_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_carbon_entries/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_carbon_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_carbon_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_carbon_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_carbon_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_carbon_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_carbon_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_carbon_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_carbon_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_carbon_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_carbon_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_carbon_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_carbon_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_carbon_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_carbon_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_carbon_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_holidays', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_holidays").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_holidays/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_holidays").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_holidays").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_holidays/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_holidays").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_holidays/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_holidays").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_holidays/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_holidays").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_holidays").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_holidays").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_holidays").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_holidays").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_holidays").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_holidays").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_holidays").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_holidays").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_holidays").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_holidays").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_holidays").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_holidays").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_holidays").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_holidays").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_members', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_members/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_members").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_members/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_members/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_members/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_members").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_members").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_members").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_rfi_custom_fields', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_rfi_custom_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_custom_fields/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_custom_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_custom_fields").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_rfi_custom_fields/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_custom_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_custom_fields/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_custom_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_custom_fields/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_rfi_custom_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_custom_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_custom_fields").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_custom_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_custom_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_rfi_custom_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_custom_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_custom_fields").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_custom_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_custom_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_rfi_custom_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_custom_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_custom_fields").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_custom_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_custom_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_rfi_notification_prefs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_rfi_notification_prefs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_notification_prefs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_notification_prefs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_notification_prefs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_rfi_notification_prefs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_notification_prefs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_notification_prefs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_notification_prefs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_notification_prefs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_rfi_notification_prefs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_notification_prefs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_notification_prefs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_notification_prefs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_notification_prefs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_rfi_notification_prefs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_notification_prefs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_notification_prefs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_notification_prefs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_notification_prefs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_rfi_notification_prefs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_notification_prefs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_notification_prefs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_notification_prefs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_notification_prefs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_rfi_permissions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_rfi_permissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_permissions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_permissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_permissions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_rfi_permissions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_permissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_permissions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_permissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_permissions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_rfi_permissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_permissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_permissions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_permissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_permissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_rfi_permissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_permissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_permissions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_permissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_permissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_rfi_permissions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_permissions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_permissions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_permissions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_permissions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_rfi_response_types', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_rfi_response_types").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_response_types/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_response_types").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_response_types").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_rfi_response_types/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_response_types").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_response_types/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_response_types").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_response_types/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_rfi_response_types").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_response_types").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_response_types").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_response_types").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_response_types").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_rfi_response_types").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_response_types").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_response_types").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_response_types").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_response_types").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_rfi_response_types").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_response_types").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_response_types").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_response_types").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_response_types").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_rfi_settings', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_rfi_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_settings/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_settings").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_rfi_settings/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_settings/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_settings/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_rfi_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_settings").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_rfi_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_settings").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_rfi_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_settings").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_rfi_workflows', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_rfi_workflows").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_workflows/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_workflows").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_workflows").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_rfi_workflows/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_workflows").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_workflows/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_workflows").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_rfi_workflows/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_rfi_workflows").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_workflows").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_workflows").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_workflows").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_workflows").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_rfi_workflows").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_workflows").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_workflows").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_workflows").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_workflows").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_rfi_workflows").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_rfi_workflows").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_rfi_workflows").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_rfi_workflows").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_rfi_workflows").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_role_groups', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_role_groups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_role_groups/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_role_groups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_role_groups").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_role_groups/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_role_groups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_role_groups/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_role_groups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_role_groups/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_role_groups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_role_groups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_role_groups").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_role_groups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_role_groups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_role_groups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_role_groups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_role_groups").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_role_groups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_role_groups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_role_groups").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_role_groups").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_role_groups").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_role_groups").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_role_groups").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_snapshots', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_snapshots").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_snapshots/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_snapshots").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_snapshots").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_snapshots/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_snapshots").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_snapshots/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_snapshots").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_snapshots/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_snapshots").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_snapshots").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_snapshots").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_snapshots").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_snapshots").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_snapshots").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_snapshots").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_snapshots").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_snapshots").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_snapshots").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_snapshots").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_snapshots").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_snapshots").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_snapshots").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_snapshots").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_spec_book', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_spec_book").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_spec_book/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_spec_book").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_spec_book").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_spec_book/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_spec_book").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_spec_book/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_spec_book").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_spec_book/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_spec_book").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_spec_book").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_spec_book").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_spec_book").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_spec_book").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_spec_book").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_spec_book").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_spec_book").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_spec_book").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_spec_book").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_spec_book").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_spec_book").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_spec_book").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_spec_book").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_spec_book").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: project_templates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("project_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_templates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_templates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'project_templates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_templates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'project_templates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("project_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("project_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("project_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("project_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("project_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("project_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("project_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: projects', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("projects").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'projects/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("projects").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("projects").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'projects/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("projects").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'projects/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("projects").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'projects/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("projects").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("projects").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("projects").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("projects").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("projects").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("projects").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("projects").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("projects").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("projects").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("projects").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("projects").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("projects").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("projects").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("projects").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("projects").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: punch_item_comments', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("punch_item_comments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'punch_item_comments/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("punch_item_comments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("punch_item_comments").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'punch_item_comments/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("punch_item_comments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'punch_item_comments/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("punch_item_comments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'punch_item_comments/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("punch_item_comments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("punch_item_comments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("punch_item_comments").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("punch_item_comments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("punch_item_comments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("punch_item_comments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("punch_item_comments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("punch_item_comments").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("punch_item_comments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("punch_item_comments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("punch_item_comments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("punch_item_comments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("punch_item_comments").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("punch_item_comments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("punch_item_comments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: punch_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("punch_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'punch_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("punch_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("punch_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'punch_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("punch_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'punch_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("punch_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'punch_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("punch_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("punch_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("punch_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("punch_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("punch_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("punch_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("punch_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("punch_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("punch_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("punch_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("punch_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("punch_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("punch_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("punch_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("punch_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: purchase_orders', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("purchase_orders").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'purchase_orders/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("purchase_orders").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("purchase_orders").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'purchase_orders/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("purchase_orders").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'purchase_orders/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("purchase_orders").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'purchase_orders/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("purchase_orders").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("purchase_orders").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("purchase_orders").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("purchase_orders").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("purchase_orders").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("purchase_orders").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("purchase_orders").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("purchase_orders").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("purchase_orders").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("purchase_orders").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("purchase_orders").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("purchase_orders").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("purchase_orders").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("purchase_orders").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("purchase_orders").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rate_limit_buckets', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rate_limit_buckets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rate_limit_buckets/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rate_limit_buckets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rate_limit_buckets").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rate_limit_buckets/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rate_limit_buckets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rate_limit_buckets/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rate_limit_buckets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rate_limit_buckets/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rate_limit_buckets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rate_limit_buckets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rate_limit_buckets").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rate_limit_buckets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rate_limit_buckets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rate_limit_buckets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rate_limit_buckets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rate_limit_buckets").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rate_limit_buckets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rate_limit_buckets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rate_limit_buckets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rate_limit_buckets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rate_limit_buckets").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rate_limit_buckets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rate_limit_buckets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rate_limit_overrides', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rate_limit_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rate_limit_overrides/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rate_limit_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rate_limit_overrides").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rate_limit_overrides/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rate_limit_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rate_limit_overrides/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rate_limit_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rate_limit_overrides/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rate_limit_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rate_limit_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rate_limit_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rate_limit_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rate_limit_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rate_limit_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rate_limit_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rate_limit_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rate_limit_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rate_limit_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rate_limit_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rate_limit_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rate_limit_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rate_limit_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rate_limit_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: report_runs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("report_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'report_runs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_runs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'report_runs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'report_runs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'report_runs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("report_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("report_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("report_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: report_schedules', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("report_schedules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'report_schedules/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_schedules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_schedules").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'report_schedules/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_schedules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'report_schedules/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_schedules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'report_schedules/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("report_schedules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_schedules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_schedules").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_schedules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_schedules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("report_schedules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_schedules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_schedules").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_schedules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_schedules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("report_schedules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_schedules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_schedules").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_schedules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_schedules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: report_templates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("report_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'report_templates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_templates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'report_templates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'report_templates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'report_templates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("report_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("report_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("report_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("report_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("report_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("report_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("report_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: resend_webhook_events', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("resend_webhook_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'resend_webhook_events/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("resend_webhook_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("resend_webhook_events").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'resend_webhook_events/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("resend_webhook_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'resend_webhook_events/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("resend_webhook_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'resend_webhook_events/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("resend_webhook_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("resend_webhook_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("resend_webhook_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("resend_webhook_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("resend_webhook_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("resend_webhook_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("resend_webhook_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("resend_webhook_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("resend_webhook_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("resend_webhook_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("resend_webhook_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("resend_webhook_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("resend_webhook_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("resend_webhook_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("resend_webhook_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: retainage_entries', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("retainage_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'retainage_entries/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("retainage_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("retainage_entries").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'retainage_entries/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("retainage_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'retainage_entries/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("retainage_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'retainage_entries/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("retainage_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("retainage_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("retainage_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("retainage_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("retainage_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("retainage_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("retainage_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("retainage_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("retainage_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("retainage_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("retainage_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("retainage_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("retainage_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("retainage_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("retainage_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: retainage_ledger', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("retainage_ledger").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'retainage_ledger/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("retainage_ledger").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("retainage_ledger").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'retainage_ledger/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("retainage_ledger").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'retainage_ledger/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("retainage_ledger").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'retainage_ledger/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("retainage_ledger").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("retainage_ledger").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("retainage_ledger").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("retainage_ledger").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("retainage_ledger").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("retainage_ledger").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("retainage_ledger").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("retainage_ledger").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("retainage_ledger").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("retainage_ledger").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("retainage_ledger").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("retainage_ledger").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("retainage_ledger").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("retainage_ledger").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("retainage_ledger").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_assignees', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_assignees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_assignees/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_assignees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_assignees").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_assignees/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_assignees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_assignees/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_assignees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_assignees/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_assignees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_assignees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_assignees").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_assignees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_assignees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_assignees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_assignees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_assignees").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_assignees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_assignees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_assignees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_assignees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_assignees").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_assignees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_assignees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_attachments', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_attachments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_attachments/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_attachments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_attachments").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_attachments/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_attachments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_attachments/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_attachments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_attachments/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_attachments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_attachments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_attachments").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_attachments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_attachments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_attachments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_attachments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_attachments").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_attachments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_attachments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_attachments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_attachments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_attachments").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_attachments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_attachments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_clock_events', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_clock_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_clock_events/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_clock_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_clock_events").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_clock_events/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_clock_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_clock_events/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_clock_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_clock_events/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_clock_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_clock_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_clock_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_clock_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_clock_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_clock_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_clock_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_clock_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_clock_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_clock_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_clock_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_clock_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_clock_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_clock_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_clock_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_custom_reports', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_custom_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_custom_reports/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_custom_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_custom_reports").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_custom_reports/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_custom_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_custom_reports/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_custom_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_custom_reports/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_custom_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_custom_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_custom_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_custom_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_custom_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_custom_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_custom_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_custom_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_custom_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_custom_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_custom_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_custom_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_custom_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_custom_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_custom_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_custom_values', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_custom_values").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_custom_values/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_custom_values").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_custom_values").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_custom_values/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_custom_values").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_custom_values/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_custom_values").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_custom_values/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_custom_values").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_custom_values").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_custom_values").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_custom_values").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_custom_values").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_custom_values").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_custom_values").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_custom_values").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_custom_values").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_custom_values").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_custom_values").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_custom_values").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_custom_values").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_custom_values").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_custom_values").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_distributions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_distributions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_distributions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_distributions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_distributions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_distributions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_distributions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_distributions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_distributions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_drafts', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_drafts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_drafts/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_drafts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_drafts").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_drafts/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_drafts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_drafts/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_drafts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_drafts/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_drafts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_drafts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_drafts").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_drafts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_drafts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_drafts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_drafts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_drafts").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_drafts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_drafts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_drafts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_drafts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_drafts").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_drafts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_drafts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_drawing_pins', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_drawing_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_drawing_pins/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_drawing_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_drawing_pins").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_drawing_pins/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_drawing_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_drawing_pins/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_drawing_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_drawing_pins/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_drawing_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_drawing_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_drawing_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_drawing_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_drawing_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_drawing_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_drawing_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_drawing_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_drawing_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_drawing_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_drawing_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_drawing_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_drawing_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_drawing_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_drawing_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_escalations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_escalations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_escalations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_escalations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_escalations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_escalations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_escalations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_escalations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_escalations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_escalations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_escalations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_escalations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_escalations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_escalations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_escalations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_escalations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_escalations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_escalations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_escalations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_escalations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_escalations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_escalations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_escalations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_escalations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_escalations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_links', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_links/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_links").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_links/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_links/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_links/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_responses', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_responses/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_responses").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_responses/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_responses/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_responses/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_responses").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_responses").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_responses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_responses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_responses").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_responses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_responses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_responses_versions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_responses_versions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_responses_versions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_responses_versions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_responses_versions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_responses_versions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_responses_versions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_responses_versions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_responses_versions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_responses_versions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_responses_versions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_responses_versions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_responses_versions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_responses_versions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_responses_versions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_responses_versions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_responses_versions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_responses_versions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_responses_versions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_responses_versions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_responses_versions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_responses_versions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_responses_versions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_responses_versions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_responses_versions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_saved_views', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_saved_views").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_saved_views/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_saved_views").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_saved_views").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_saved_views/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_saved_views").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_saved_views/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_saved_views").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_saved_views/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_saved_views").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_saved_views").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_saved_views").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_saved_views").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_saved_views").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_saved_views").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_saved_views").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_saved_views").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_saved_views").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_saved_views").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_saved_views").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_saved_views").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_saved_views").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_saved_views").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_saved_views").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_scheduled_reports', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_scheduled_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_scheduled_reports/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_scheduled_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_scheduled_reports").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_scheduled_reports/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_scheduled_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_scheduled_reports/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_scheduled_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_scheduled_reports/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_scheduled_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_scheduled_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_scheduled_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_scheduled_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_scheduled_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_scheduled_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_scheduled_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_scheduled_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_scheduled_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_scheduled_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_scheduled_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_scheduled_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_scheduled_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_scheduled_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_scheduled_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_user_column_prefs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_user_column_prefs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_user_column_prefs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_user_column_prefs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_user_column_prefs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_user_column_prefs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_user_column_prefs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_user_column_prefs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_user_column_prefs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_user_column_prefs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_user_column_prefs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_user_column_prefs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_user_column_prefs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_user_column_prefs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_user_column_prefs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_user_column_prefs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_user_column_prefs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_user_column_prefs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_user_column_prefs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_user_column_prefs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_user_column_prefs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_user_column_prefs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_user_column_prefs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_user_column_prefs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_user_column_prefs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfi_watchers', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfi_watchers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_watchers/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_watchers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_watchers").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfi_watchers/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_watchers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_watchers/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_watchers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfi_watchers/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfi_watchers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_watchers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_watchers").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_watchers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_watchers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfi_watchers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_watchers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_watchers").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_watchers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_watchers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfi_watchers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfi_watchers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfi_watchers").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfi_watchers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfi_watchers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: rfis', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("rfis").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfis/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfis").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfis").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'rfis/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfis").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfis/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfis").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'rfis/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("rfis").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfis").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfis").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfis").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfis").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("rfis").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfis").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfis").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfis").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfis").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("rfis").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("rfis").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("rfis").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("rfis").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("rfis").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: risk_predictions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("risk_predictions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'risk_predictions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("risk_predictions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("risk_predictions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'risk_predictions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("risk_predictions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'risk_predictions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("risk_predictions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'risk_predictions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("risk_predictions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("risk_predictions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("risk_predictions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("risk_predictions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("risk_predictions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("risk_predictions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("risk_predictions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("risk_predictions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("risk_predictions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("risk_predictions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("risk_predictions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("risk_predictions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("risk_predictions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("risk_predictions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("risk_predictions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: role_to_default_persona', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("role_to_default_persona").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'role_to_default_persona/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("role_to_default_persona").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("role_to_default_persona").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'role_to_default_persona/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("role_to_default_persona").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'role_to_default_persona/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("role_to_default_persona").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'role_to_default_persona/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("role_to_default_persona").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("role_to_default_persona").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("role_to_default_persona").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("role_to_default_persona").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("role_to_default_persona").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("role_to_default_persona").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("role_to_default_persona").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("role_to_default_persona").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("role_to_default_persona").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("role_to_default_persona").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("role_to_default_persona").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("role_to_default_persona").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("role_to_default_persona").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("role_to_default_persona").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("role_to_default_persona").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: safety_certifications', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("safety_certifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_certifications/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_certifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_certifications").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'safety_certifications/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_certifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_certifications/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_certifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_certifications/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("safety_certifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_certifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_certifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_certifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_certifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("safety_certifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_certifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_certifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_certifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_certifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("safety_certifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_certifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_certifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_certifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_certifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: safety_inspection_templates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("safety_inspection_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_inspection_templates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_inspection_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_inspection_templates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'safety_inspection_templates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_inspection_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_inspection_templates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_inspection_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_inspection_templates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("safety_inspection_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_inspection_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_inspection_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_inspection_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_inspection_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("safety_inspection_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_inspection_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_inspection_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_inspection_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_inspection_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("safety_inspection_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_inspection_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_inspection_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_inspection_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_inspection_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: safety_inspections', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("safety_inspections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_inspections/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_inspections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_inspections").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'safety_inspections/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_inspections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_inspections/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_inspections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_inspections/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("safety_inspections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_inspections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_inspections").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_inspections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_inspections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("safety_inspections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_inspections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_inspections").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_inspections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_inspections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("safety_inspections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_inspections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_inspections").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_inspections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_inspections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: safety_observations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("safety_observations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_observations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_observations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_observations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'safety_observations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_observations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_observations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_observations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_observations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("safety_observations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_observations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_observations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_observations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_observations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("safety_observations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_observations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_observations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_observations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_observations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("safety_observations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_observations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_observations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_observations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_observations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: safety_photo_analyses', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("safety_photo_analyses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_photo_analyses/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_photo_analyses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_photo_analyses").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'safety_photo_analyses/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_photo_analyses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_photo_analyses/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_photo_analyses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'safety_photo_analyses/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("safety_photo_analyses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_photo_analyses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_photo_analyses").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_photo_analyses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_photo_analyses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("safety_photo_analyses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_photo_analyses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_photo_analyses").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_photo_analyses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_photo_analyses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("safety_photo_analyses").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("safety_photo_analyses").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("safety_photo_analyses").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("safety_photo_analyses").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("safety_photo_analyses").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: schedule_import_jobs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("schedule_import_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_import_jobs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_import_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_import_jobs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'schedule_import_jobs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_import_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_import_jobs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_import_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_import_jobs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("schedule_import_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_import_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_import_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_import_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_import_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("schedule_import_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_import_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_import_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_import_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_import_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("schedule_import_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_import_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_import_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_import_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_import_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: schedule_of_values', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("schedule_of_values").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_of_values/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_of_values").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_of_values").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'schedule_of_values/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_of_values").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_of_values/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_of_values").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_of_values/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("schedule_of_values").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_of_values").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_of_values").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_of_values").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_of_values").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("schedule_of_values").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_of_values").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_of_values").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_of_values").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_of_values").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("schedule_of_values").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_of_values").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_of_values").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_of_values").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_of_values").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: schedule_phases', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("schedule_phases").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_phases/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_phases").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_phases").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'schedule_phases/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_phases").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_phases/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_phases").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_phases/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("schedule_phases").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_phases").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_phases").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_phases").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_phases").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("schedule_phases").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_phases").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_phases").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_phases").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_phases").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("schedule_phases").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_phases").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_phases").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_phases").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_phases").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: schedule_recovery_events', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("schedule_recovery_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_recovery_events/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_recovery_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_recovery_events").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'schedule_recovery_events/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_recovery_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_recovery_events/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_recovery_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'schedule_recovery_events/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("schedule_recovery_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_recovery_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_recovery_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_recovery_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_recovery_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("schedule_recovery_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_recovery_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_recovery_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_recovery_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_recovery_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("schedule_recovery_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("schedule_recovery_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("schedule_recovery_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("schedule_recovery_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("schedule_recovery_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: scheduled_insights_log', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("scheduled_insights_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'scheduled_insights_log/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("scheduled_insights_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("scheduled_insights_log").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'scheduled_insights_log/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("scheduled_insights_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'scheduled_insights_log/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("scheduled_insights_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'scheduled_insights_log/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("scheduled_insights_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("scheduled_insights_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("scheduled_insights_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("scheduled_insights_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("scheduled_insights_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("scheduled_insights_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("scheduled_insights_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("scheduled_insights_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("scheduled_insights_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("scheduled_insights_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("scheduled_insights_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("scheduled_insights_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("scheduled_insights_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("scheduled_insights_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("scheduled_insights_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: search_index_dirty_flags', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("search_index_dirty_flags").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'search_index_dirty_flags/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("search_index_dirty_flags").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("search_index_dirty_flags").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'search_index_dirty_flags/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("search_index_dirty_flags").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'search_index_dirty_flags/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("search_index_dirty_flags").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'search_index_dirty_flags/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("search_index_dirty_flags").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("search_index_dirty_flags").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("search_index_dirty_flags").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("search_index_dirty_flags").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("search_index_dirty_flags").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("search_index_dirty_flags").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("search_index_dirty_flags").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("search_index_dirty_flags").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("search_index_dirty_flags").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("search_index_dirty_flags").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("search_index_dirty_flags").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("search_index_dirty_flags").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("search_index_dirty_flags").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("search_index_dirty_flags").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("search_index_dirty_flags").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: signature_fields', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("signature_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'signature_fields/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("signature_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("signature_fields").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'signature_fields/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("signature_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'signature_fields/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("signature_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'signature_fields/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("signature_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("signature_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("signature_fields").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("signature_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("signature_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("signature_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("signature_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("signature_fields").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("signature_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("signature_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("signature_fields").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("signature_fields").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("signature_fields").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("signature_fields").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("signature_fields").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: site_check_ins', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("site_check_ins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_check_ins/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_check_ins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_check_ins").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'site_check_ins/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_check_ins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_check_ins/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_check_ins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_check_ins/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("site_check_ins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_check_ins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_check_ins").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_check_ins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_check_ins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("site_check_ins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_check_ins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_check_ins").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_check_ins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_check_ins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("site_check_ins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_check_ins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_check_ins").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_check_ins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_check_ins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: site_map_pins', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("site_map_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_map_pins/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_map_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_map_pins").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'site_map_pins/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_map_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_map_pins/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_map_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_map_pins/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("site_map_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_map_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_map_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_map_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_map_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("site_map_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_map_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_map_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_map_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_map_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("site_map_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_map_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_map_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_map_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_map_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: site_map_zones', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("site_map_zones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_map_zones/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_map_zones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_map_zones").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'site_map_zones/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_map_zones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_map_zones/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_map_zones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_map_zones/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("site_map_zones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_map_zones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_map_zones").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_map_zones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_map_zones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("site_map_zones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_map_zones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_map_zones").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_map_zones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_map_zones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("site_map_zones").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_map_zones").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_map_zones").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_map_zones").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_map_zones").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: site_plans', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("site_plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_plans/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_plans").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'site_plans/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_plans/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'site_plans/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("site_plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_plans").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("site_plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_plans").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("site_plans").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("site_plans").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("site_plans").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("site_plans").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("site_plans").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: slack_delivery_log', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("slack_delivery_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'slack_delivery_log/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("slack_delivery_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("slack_delivery_log").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'slack_delivery_log/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("slack_delivery_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'slack_delivery_log/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("slack_delivery_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'slack_delivery_log/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("slack_delivery_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("slack_delivery_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("slack_delivery_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("slack_delivery_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("slack_delivery_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("slack_delivery_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("slack_delivery_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("slack_delivery_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("slack_delivery_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("slack_delivery_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("slack_delivery_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("slack_delivery_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("slack_delivery_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("slack_delivery_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("slack_delivery_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: spec_sections', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("spec_sections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'spec_sections/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("spec_sections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("spec_sections").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'spec_sections/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("spec_sections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'spec_sections/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("spec_sections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'spec_sections/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("spec_sections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("spec_sections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("spec_sections").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("spec_sections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("spec_sections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("spec_sections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("spec_sections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("spec_sections").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("spec_sections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("spec_sections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("spec_sections").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("spec_sections").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("spec_sections").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("spec_sections").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("spec_sections").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: specifications', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("specifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'specifications/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("specifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("specifications").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'specifications/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("specifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'specifications/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("specifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'specifications/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("specifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("specifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("specifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("specifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("specifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("specifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("specifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("specifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("specifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("specifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("specifications").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("specifications").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("specifications").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("specifications").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("specifications").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: sso_configurations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("sso_configurations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'sso_configurations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sso_configurations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sso_configurations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'sso_configurations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sso_configurations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'sso_configurations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sso_configurations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'sso_configurations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("sso_configurations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sso_configurations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sso_configurations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sso_configurations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sso_configurations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("sso_configurations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sso_configurations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sso_configurations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sso_configurations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sso_configurations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("sso_configurations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sso_configurations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sso_configurations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sso_configurations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sso_configurations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: sso_login_events', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("sso_login_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'sso_login_events/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sso_login_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sso_login_events").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'sso_login_events/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sso_login_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'sso_login_events/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sso_login_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'sso_login_events/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("sso_login_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sso_login_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sso_login_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sso_login_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sso_login_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("sso_login_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sso_login_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sso_login_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sso_login_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sso_login_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("sso_login_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sso_login_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sso_login_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sso_login_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sso_login_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: state_lien_rules', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("state_lien_rules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'state_lien_rules/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("state_lien_rules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("state_lien_rules").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'state_lien_rules/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("state_lien_rules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'state_lien_rules/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("state_lien_rules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'state_lien_rules/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("state_lien_rules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("state_lien_rules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("state_lien_rules").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("state_lien_rules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("state_lien_rules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("state_lien_rules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("state_lien_rules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("state_lien_rules").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("state_lien_rules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("state_lien_rules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("state_lien_rules").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("state_lien_rules").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("state_lien_rules").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("state_lien_rules").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("state_lien_rules").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: storage_backup_log', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("storage_backup_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'storage_backup_log/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("storage_backup_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("storage_backup_log").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'storage_backup_log/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("storage_backup_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'storage_backup_log/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("storage_backup_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'storage_backup_log/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("storage_backup_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("storage_backup_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("storage_backup_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("storage_backup_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("storage_backup_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("storage_backup_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("storage_backup_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("storage_backup_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("storage_backup_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("storage_backup_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("storage_backup_log").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("storage_backup_log").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("storage_backup_log").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("storage_backup_log").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("storage_backup_log").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: stripe_connected_accounts', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("stripe_connected_accounts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'stripe_connected_accounts/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("stripe_connected_accounts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("stripe_connected_accounts").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'stripe_connected_accounts/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("stripe_connected_accounts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'stripe_connected_accounts/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("stripe_connected_accounts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'stripe_connected_accounts/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("stripe_connected_accounts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("stripe_connected_accounts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("stripe_connected_accounts").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("stripe_connected_accounts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("stripe_connected_accounts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("stripe_connected_accounts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("stripe_connected_accounts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("stripe_connected_accounts").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("stripe_connected_accounts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("stripe_connected_accounts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("stripe_connected_accounts").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("stripe_connected_accounts").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("stripe_connected_accounts").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("stripe_connected_accounts").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("stripe_connected_accounts").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: stripe_processed_events', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("stripe_processed_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'stripe_processed_events/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("stripe_processed_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("stripe_processed_events").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'stripe_processed_events/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("stripe_processed_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'stripe_processed_events/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("stripe_processed_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'stripe_processed_events/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("stripe_processed_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("stripe_processed_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("stripe_processed_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("stripe_processed_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("stripe_processed_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("stripe_processed_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("stripe_processed_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("stripe_processed_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("stripe_processed_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("stripe_processed_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("stripe_processed_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("stripe_processed_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("stripe_processed_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("stripe_processed_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("stripe_processed_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: subcontractor_invoices', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("subcontractor_invoices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'subcontractor_invoices/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subcontractor_invoices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subcontractor_invoices").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'subcontractor_invoices/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subcontractor_invoices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'subcontractor_invoices/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subcontractor_invoices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'subcontractor_invoices/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("subcontractor_invoices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subcontractor_invoices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subcontractor_invoices").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subcontractor_invoices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subcontractor_invoices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("subcontractor_invoices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subcontractor_invoices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subcontractor_invoices").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subcontractor_invoices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subcontractor_invoices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("subcontractor_invoices").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subcontractor_invoices").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subcontractor_invoices").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subcontractor_invoices").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subcontractor_invoices").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: subcontractor_ratings', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("subcontractor_ratings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'subcontractor_ratings/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subcontractor_ratings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subcontractor_ratings").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'subcontractor_ratings/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subcontractor_ratings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'subcontractor_ratings/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subcontractor_ratings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'subcontractor_ratings/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("subcontractor_ratings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subcontractor_ratings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subcontractor_ratings").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subcontractor_ratings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subcontractor_ratings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("subcontractor_ratings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subcontractor_ratings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subcontractor_ratings").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subcontractor_ratings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subcontractor_ratings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("subcontractor_ratings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subcontractor_ratings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subcontractor_ratings").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subcontractor_ratings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subcontractor_ratings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_approvals', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_approvals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_approvals/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_approvals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_approvals").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_approvals/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_approvals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_approvals/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_approvals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_approvals/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_approvals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_approvals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_approvals").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_approvals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_approvals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_approvals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_approvals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_approvals").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_approvals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_approvals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_approvals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_approvals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_approvals").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_approvals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_approvals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_change_history', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_change_history").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_change_history/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_change_history").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_change_history").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_change_history/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_change_history").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_change_history/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_change_history").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_change_history/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_change_history").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_change_history").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_change_history").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_change_history").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_change_history").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_change_history").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_change_history").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_change_history").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_change_history").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_change_history").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_change_history").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_change_history").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_change_history").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_change_history").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_change_history").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_distributions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_distributions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_distributions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_distributions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_distributions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_distributions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_distributions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_distributions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_distributions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_distributions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_distributions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_distributions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_distributions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_drawing_pins', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_drawing_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_drawing_pins/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_drawing_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_drawing_pins").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_drawing_pins/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_drawing_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_drawing_pins/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_drawing_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_drawing_pins/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_drawing_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_drawing_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_drawing_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_drawing_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_drawing_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_drawing_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_drawing_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_drawing_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_drawing_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_drawing_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_drawing_pins").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_drawing_pins").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_drawing_pins").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_drawing_pins").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_drawing_pins").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_emails', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_emails").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_emails/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_emails").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_emails").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_emails/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_emails").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_emails/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_emails").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_emails/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_emails").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_emails").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_emails").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_emails").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_emails").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_emails").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_emails").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_emails").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_emails").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_emails").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_emails").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_emails").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_emails").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_emails").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_emails").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_magic_links', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_magic_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_magic_links/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_magic_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_magic_links").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_magic_links/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_magic_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_magic_links/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_magic_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_magic_links/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_magic_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_magic_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_magic_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_magic_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_magic_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_magic_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_magic_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_magic_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_magic_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_magic_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_magic_links").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_magic_links").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_magic_links").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_magic_links").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_magic_links").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_markup', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_markup").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_markup/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_markup").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_markup").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_markup/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_markup").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_markup/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_markup").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_markup/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_markup").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_markup").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_markup").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_markup").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_markup").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_markup").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_markup").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_markup").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_markup").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_markup").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_markup").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_markup").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_markup").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_markup").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_markup").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_packages', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_packages/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_packages").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_packages/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_packages/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_packages/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_packages").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_packages").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_packages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_packages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_packages").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_packages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_packages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_reviewers', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_reviewers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_reviewers/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_reviewers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_reviewers").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_reviewers/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_reviewers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_reviewers/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_reviewers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_reviewers/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_reviewers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_reviewers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_reviewers").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_reviewers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_reviewers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_reviewers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_reviewers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_reviewers").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_reviewers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_reviewers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_reviewers").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_reviewers").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_reviewers").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_reviewers").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_reviewers").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_revisions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_revisions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_revisions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_revisions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_revisions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_revisions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_revisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_revisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_revisions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_revisions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_revisions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_revisions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_revisions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_saved_views', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_saved_views").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_saved_views/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_saved_views").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_saved_views").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_saved_views/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_saved_views").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_saved_views/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_saved_views").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_saved_views/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_saved_views").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_saved_views").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_saved_views").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_saved_views").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_saved_views").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_saved_views").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_saved_views").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_saved_views").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_saved_views").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_saved_views").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_saved_views").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_saved_views").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_saved_views").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_saved_views").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_saved_views").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_settings', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_settings/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_settings").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_settings/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_settings/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_settings/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_settings").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_settings").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_settings").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_settings").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_settings").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_settings").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_settings").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittal_workflow_templates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittal_workflow_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_workflow_templates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_workflow_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_workflow_templates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittal_workflow_templates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_workflow_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_workflow_templates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_workflow_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittal_workflow_templates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittal_workflow_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_workflow_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_workflow_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_workflow_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_workflow_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittal_workflow_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_workflow_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_workflow_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_workflow_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_workflow_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittal_workflow_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittal_workflow_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittal_workflow_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittal_workflow_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittal_workflow_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: submittals', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("submittals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittals/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittals").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'submittals/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittals/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'submittals/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("submittals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittals").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("submittals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittals").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("submittals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("submittals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("submittals").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("submittals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("submittals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: subscriptions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("subscriptions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'subscriptions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subscriptions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subscriptions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'subscriptions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subscriptions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'subscriptions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subscriptions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'subscriptions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("subscriptions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subscriptions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subscriptions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subscriptions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subscriptions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("subscriptions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subscriptions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subscriptions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subscriptions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subscriptions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("subscriptions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("subscriptions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("subscriptions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("subscriptions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("subscriptions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: sustainability_metrics', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("sustainability_metrics").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'sustainability_metrics/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sustainability_metrics").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sustainability_metrics").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'sustainability_metrics/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sustainability_metrics").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'sustainability_metrics/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sustainability_metrics").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'sustainability_metrics/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("sustainability_metrics").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sustainability_metrics").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sustainability_metrics").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sustainability_metrics").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sustainability_metrics").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("sustainability_metrics").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sustainability_metrics").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sustainability_metrics").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sustainability_metrics").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sustainability_metrics").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("sustainability_metrics").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("sustainability_metrics").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("sustainability_metrics").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("sustainability_metrics").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("sustainability_metrics").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: takeoff_items', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("takeoff_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'takeoff_items/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("takeoff_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("takeoff_items").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'takeoff_items/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("takeoff_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'takeoff_items/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("takeoff_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'takeoff_items/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("takeoff_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("takeoff_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("takeoff_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("takeoff_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("takeoff_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("takeoff_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("takeoff_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("takeoff_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("takeoff_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("takeoff_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("takeoff_items").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("takeoff_items").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("takeoff_items").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("takeoff_items").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("takeoff_items").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: task_templates', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("task_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'task_templates/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("task_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("task_templates").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'task_templates/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("task_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'task_templates/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("task_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'task_templates/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("task_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("task_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("task_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("task_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("task_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("task_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("task_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("task_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("task_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("task_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("task_templates").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("task_templates").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("task_templates").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("task_templates").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("task_templates").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: tasks', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("tasks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'tasks/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("tasks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("tasks").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'tasks/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("tasks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'tasks/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("tasks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'tasks/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("tasks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("tasks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("tasks").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("tasks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("tasks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("tasks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("tasks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("tasks").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("tasks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("tasks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("tasks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("tasks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("tasks").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("tasks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("tasks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: time_entries', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("time_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'time_entries/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("time_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("time_entries").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'time_entries/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("time_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'time_entries/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("time_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'time_entries/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("time_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("time_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("time_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("time_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("time_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("time_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("time_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("time_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("time_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("time_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("time_entries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("time_entries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("time_entries").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("time_entries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("time_entries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: timesheets', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("timesheets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'timesheets/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("timesheets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("timesheets").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'timesheets/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("timesheets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'timesheets/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("timesheets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'timesheets/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("timesheets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("timesheets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("timesheets").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("timesheets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("timesheets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("timesheets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("timesheets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("timesheets").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("timesheets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("timesheets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("timesheets").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("timesheets").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("timesheets").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("timesheets").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("timesheets").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: toolbox_talk_attendees', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("toolbox_talk_attendees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'toolbox_talk_attendees/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("toolbox_talk_attendees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("toolbox_talk_attendees").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'toolbox_talk_attendees/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("toolbox_talk_attendees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'toolbox_talk_attendees/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("toolbox_talk_attendees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'toolbox_talk_attendees/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("toolbox_talk_attendees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("toolbox_talk_attendees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("toolbox_talk_attendees").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("toolbox_talk_attendees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("toolbox_talk_attendees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("toolbox_talk_attendees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("toolbox_talk_attendees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("toolbox_talk_attendees").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("toolbox_talk_attendees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("toolbox_talk_attendees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("toolbox_talk_attendees").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("toolbox_talk_attendees").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("toolbox_talk_attendees").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("toolbox_talk_attendees").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("toolbox_talk_attendees").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: toolbox_talks', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("toolbox_talks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'toolbox_talks/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("toolbox_talks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("toolbox_talks").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'toolbox_talks/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("toolbox_talks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'toolbox_talks/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("toolbox_talks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'toolbox_talks/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("toolbox_talks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("toolbox_talks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("toolbox_talks").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("toolbox_talks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("toolbox_talks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("toolbox_talks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("toolbox_talks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("toolbox_talks").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("toolbox_talks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("toolbox_talks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("toolbox_talks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("toolbox_talks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("toolbox_talks").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("toolbox_talks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("toolbox_talks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: transmittals', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("transmittals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'transmittals/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("transmittals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("transmittals").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'transmittals/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("transmittals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'transmittals/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("transmittals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'transmittals/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("transmittals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("transmittals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("transmittals").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("transmittals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("transmittals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("transmittals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("transmittals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("transmittals").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("transmittals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("transmittals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("transmittals").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("transmittals").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("transmittals").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("transmittals").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("transmittals").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: typing_indicators', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("typing_indicators").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'typing_indicators/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("typing_indicators").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("typing_indicators").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'typing_indicators/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("typing_indicators").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'typing_indicators/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("typing_indicators").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'typing_indicators/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("typing_indicators").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("typing_indicators").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("typing_indicators").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("typing_indicators").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("typing_indicators").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("typing_indicators").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("typing_indicators").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("typing_indicators").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("typing_indicators").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("typing_indicators").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("typing_indicators").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("typing_indicators").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("typing_indicators").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("typing_indicators").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("typing_indicators").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: usage_events', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("usage_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'usage_events/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("usage_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("usage_events").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'usage_events/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("usage_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'usage_events/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("usage_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'usage_events/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("usage_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("usage_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("usage_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("usage_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("usage_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("usage_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("usage_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("usage_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("usage_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("usage_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("usage_events").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("usage_events").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("usage_events").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("usage_events").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("usage_events").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: user_invitations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("user_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'user_invitations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("user_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("user_invitations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'user_invitations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("user_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'user_invitations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("user_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'user_invitations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("user_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("user_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("user_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("user_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("user_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("user_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("user_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("user_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("user_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("user_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("user_invitations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("user_invitations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("user_invitations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("user_invitations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("user_invitations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: user_rfi_notification_overrides', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("user_rfi_notification_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'user_rfi_notification_overrides/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("user_rfi_notification_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("user_rfi_notification_overrides").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'user_rfi_notification_overrides/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("user_rfi_notification_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'user_rfi_notification_overrides/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("user_rfi_notification_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'user_rfi_notification_overrides/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("user_rfi_notification_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("user_rfi_notification_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("user_rfi_notification_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("user_rfi_notification_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("user_rfi_notification_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("user_rfi_notification_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("user_rfi_notification_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("user_rfi_notification_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("user_rfi_notification_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("user_rfi_notification_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("user_rfi_notification_overrides").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("user_rfi_notification_overrides").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("user_rfi_notification_overrides").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("user_rfi_notification_overrides").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("user_rfi_notification_overrides").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: vendor_evaluations', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("vendor_evaluations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'vendor_evaluations/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("vendor_evaluations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("vendor_evaluations").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'vendor_evaluations/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("vendor_evaluations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'vendor_evaluations/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("vendor_evaluations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'vendor_evaluations/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("vendor_evaluations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("vendor_evaluations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("vendor_evaluations").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("vendor_evaluations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("vendor_evaluations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("vendor_evaluations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("vendor_evaluations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("vendor_evaluations").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("vendor_evaluations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("vendor_evaluations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("vendor_evaluations").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("vendor_evaluations").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("vendor_evaluations").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("vendor_evaluations").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("vendor_evaluations").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: vendors', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("vendors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'vendors/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("vendors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("vendors").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'vendors/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("vendors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'vendors/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("vendors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'vendors/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("vendors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("vendors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("vendors").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("vendors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("vendors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("vendors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("vendors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("vendors").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("vendors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("vendors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("vendors").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("vendors").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("vendors").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("vendors").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("vendors").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: view_refresh_metadata', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("view_refresh_metadata").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'view_refresh_metadata/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("view_refresh_metadata").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("view_refresh_metadata").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'view_refresh_metadata/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("view_refresh_metadata").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'view_refresh_metadata/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("view_refresh_metadata").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'view_refresh_metadata/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("view_refresh_metadata").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("view_refresh_metadata").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("view_refresh_metadata").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("view_refresh_metadata").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("view_refresh_metadata").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("view_refresh_metadata").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("view_refresh_metadata").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("view_refresh_metadata").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("view_refresh_metadata").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("view_refresh_metadata").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("view_refresh_metadata").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("view_refresh_metadata").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("view_refresh_metadata").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("view_refresh_metadata").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("view_refresh_metadata").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: walkthrough_captures', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("walkthrough_captures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'walkthrough_captures/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("walkthrough_captures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("walkthrough_captures").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'walkthrough_captures/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("walkthrough_captures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'walkthrough_captures/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("walkthrough_captures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'walkthrough_captures/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("walkthrough_captures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("walkthrough_captures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("walkthrough_captures").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("walkthrough_captures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("walkthrough_captures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("walkthrough_captures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("walkthrough_captures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("walkthrough_captures").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("walkthrough_captures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("walkthrough_captures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("walkthrough_captures").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("walkthrough_captures").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("walkthrough_captures").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("walkthrough_captures").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("walkthrough_captures").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: walkthrough_sessions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("walkthrough_sessions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'walkthrough_sessions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("walkthrough_sessions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("walkthrough_sessions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'walkthrough_sessions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("walkthrough_sessions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'walkthrough_sessions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("walkthrough_sessions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'walkthrough_sessions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("walkthrough_sessions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("walkthrough_sessions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("walkthrough_sessions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("walkthrough_sessions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("walkthrough_sessions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("walkthrough_sessions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("walkthrough_sessions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("walkthrough_sessions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("walkthrough_sessions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("walkthrough_sessions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("walkthrough_sessions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("walkthrough_sessions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("walkthrough_sessions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("walkthrough_sessions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("walkthrough_sessions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: warranties', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("warranties").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'warranties/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("warranties").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("warranties").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'warranties/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("warranties").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'warranties/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("warranties").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'warranties/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("warranties").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("warranties").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("warranties").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("warranties").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("warranties").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("warranties").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("warranties").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("warranties").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("warranties").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("warranties").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("warranties").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("warranties").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("warranties").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("warranties").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("warranties").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: warranty_claims', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("warranty_claims").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'warranty_claims/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("warranty_claims").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("warranty_claims").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'warranty_claims/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("warranty_claims").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'warranty_claims/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("warranty_claims").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'warranty_claims/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("warranty_claims").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("warranty_claims").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("warranty_claims").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("warranty_claims").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("warranty_claims").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("warranty_claims").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("warranty_claims").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("warranty_claims").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("warranty_claims").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("warranty_claims").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("warranty_claims").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("warranty_claims").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("warranty_claims").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("warranty_claims").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("warranty_claims").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: waste_logs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("waste_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'waste_logs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("waste_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("waste_logs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'waste_logs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("waste_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'waste_logs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("waste_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'waste_logs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("waste_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("waste_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("waste_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("waste_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("waste_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("waste_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("waste_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("waste_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("waste_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("waste_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("waste_logs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("waste_logs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("waste_logs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("waste_logs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("waste_logs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: weather_cache', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("weather_cache").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'weather_cache/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weather_cache").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weather_cache").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'weather_cache/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weather_cache").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'weather_cache/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weather_cache").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'weather_cache/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("weather_cache").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weather_cache").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weather_cache").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weather_cache").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weather_cache").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("weather_cache").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weather_cache").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weather_cache").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weather_cache").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weather_cache").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("weather_cache").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weather_cache").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weather_cache").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weather_cache").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weather_cache").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: weather_records', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("weather_records").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'weather_records/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weather_records").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weather_records").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'weather_records/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weather_records").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'weather_records/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weather_records").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'weather_records/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("weather_records").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weather_records").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weather_records").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weather_records").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weather_records").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("weather_records").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weather_records").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weather_records").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weather_records").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weather_records").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("weather_records").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weather_records").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weather_records").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weather_records").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weather_records").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: webhook_deliveries', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("webhook_deliveries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'webhook_deliveries/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhook_deliveries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhook_deliveries").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'webhook_deliveries/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhook_deliveries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'webhook_deliveries/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhook_deliveries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'webhook_deliveries/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("webhook_deliveries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhook_deliveries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhook_deliveries").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhook_deliveries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhook_deliveries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("webhook_deliveries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhook_deliveries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhook_deliveries").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhook_deliveries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhook_deliveries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("webhook_deliveries").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhook_deliveries").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhook_deliveries").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhook_deliveries").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhook_deliveries").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: webhook_endpoints', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("webhook_endpoints").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'webhook_endpoints/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhook_endpoints").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhook_endpoints").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'webhook_endpoints/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhook_endpoints").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'webhook_endpoints/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhook_endpoints").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'webhook_endpoints/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("webhook_endpoints").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhook_endpoints").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhook_endpoints").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhook_endpoints").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhook_endpoints").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("webhook_endpoints").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhook_endpoints").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhook_endpoints").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhook_endpoints").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhook_endpoints").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("webhook_endpoints").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhook_endpoints").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhook_endpoints").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhook_endpoints").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhook_endpoints").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: webhooks', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("webhooks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'webhooks/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhooks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhooks").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'webhooks/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhooks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'webhooks/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhooks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'webhooks/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("webhooks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhooks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhooks").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhooks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhooks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("webhooks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhooks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhooks").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhooks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhooks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("webhooks").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("webhooks").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("webhooks").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("webhooks").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("webhooks").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: weekly_commitments', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("weekly_commitments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'weekly_commitments/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weekly_commitments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weekly_commitments").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'weekly_commitments/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weekly_commitments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'weekly_commitments/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weekly_commitments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'weekly_commitments/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("weekly_commitments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weekly_commitments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weekly_commitments").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weekly_commitments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weekly_commitments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("weekly_commitments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weekly_commitments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weekly_commitments").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weekly_commitments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weekly_commitments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("weekly_commitments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("weekly_commitments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("weekly_commitments").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("weekly_commitments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("weekly_commitments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: wiki_pages', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("wiki_pages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'wiki_pages/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("wiki_pages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("wiki_pages").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'wiki_pages/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("wiki_pages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'wiki_pages/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("wiki_pages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'wiki_pages/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("wiki_pages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("wiki_pages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("wiki_pages").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("wiki_pages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("wiki_pages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("wiki_pages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("wiki_pages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("wiki_pages").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("wiki_pages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("wiki_pages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("wiki_pages").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("wiki_pages").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("wiki_pages").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("wiki_pages").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("wiki_pages").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: wip_reports', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("wip_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'wip_reports/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("wip_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("wip_reports").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'wip_reports/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("wip_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'wip_reports/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("wip_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'wip_reports/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("wip_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("wip_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("wip_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("wip_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("wip_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("wip_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("wip_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("wip_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("wip_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("wip_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("wip_reports").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("wip_reports").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("wip_reports").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("wip_reports").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("wip_reports").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: workflow_definitions', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("workflow_definitions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workflow_definitions/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workflow_definitions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workflow_definitions").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'workflow_definitions/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workflow_definitions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workflow_definitions/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workflow_definitions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workflow_definitions/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("workflow_definitions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workflow_definitions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workflow_definitions").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workflow_definitions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workflow_definitions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("workflow_definitions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workflow_definitions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workflow_definitions").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workflow_definitions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workflow_definitions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("workflow_definitions").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workflow_definitions").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workflow_definitions").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workflow_definitions").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workflow_definitions").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: workflow_runs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("workflow_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workflow_runs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workflow_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workflow_runs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'workflow_runs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workflow_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workflow_runs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workflow_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workflow_runs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("workflow_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workflow_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workflow_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workflow_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workflow_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("workflow_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workflow_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workflow_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workflow_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workflow_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("workflow_runs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workflow_runs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workflow_runs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workflow_runs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workflow_runs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: workforce_assignments', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("workforce_assignments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workforce_assignments/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workforce_assignments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workforce_assignments").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'workforce_assignments/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workforce_assignments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workforce_assignments/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workforce_assignments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workforce_assignments/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("workforce_assignments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workforce_assignments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workforce_assignments").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workforce_assignments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workforce_assignments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("workforce_assignments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workforce_assignments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workforce_assignments").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workforce_assignments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workforce_assignments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("workforce_assignments").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workforce_assignments").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workforce_assignments").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workforce_assignments").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workforce_assignments").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: workforce_members', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("workforce_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workforce_members/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workforce_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workforce_members").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'workforce_members/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workforce_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workforce_members/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workforce_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'workforce_members/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("workforce_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workforce_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workforce_members").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workforce_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workforce_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("workforce_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workforce_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workforce_members").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workforce_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workforce_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("workforce_members").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("workforce_members").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("workforce_members").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("workforce_members").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("workforce_members").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })

  describe('table: zip_upload_jobs', () => {
    it('anon / read', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      const { data, error } = await client.from("zip_upload_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('anon', outcome)
      expect(outcome, 'zip_upload_jobs/anon/read should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / insert', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("zip_upload_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('anon', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("zip_upload_jobs").delete().eq('id', inserted.id)
        }
      }
      expect(outcome, 'zip_upload_jobs/anon/insert should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / update', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("zip_upload_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('anon', outcome)
      expect(outcome, 'zip_upload_jobs/anon/update should deny but got ' + outcome).not.toBe('allow')
    })
    it('anon / delete', async () => {
      const client = _clients['anon']
      if (!client) { bucket('anon', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("zip_upload_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('anon', outcome)
      expect(outcome, 'zip_upload_jobs/anon/delete should deny but got ' + outcome).not.toBe('allow')
    })
    it('viewer / read', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      const { data, error } = await client.from("zip_upload_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / insert', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("zip_upload_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('viewer', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("zip_upload_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('viewer / update', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("zip_upload_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('viewer', outcome)
    })
    it('viewer / delete', async () => {
      const client = _clients['viewer']
      if (!client) { bucket('viewer', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("zip_upload_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('viewer', outcome)
    })
    it('project_manager / read', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      const { data, error } = await client.from("zip_upload_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / insert', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("zip_upload_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('project_manager', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("zip_upload_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('project_manager / update', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("zip_upload_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('project_manager', outcome)
    })
    it('project_manager / delete', async () => {
      const client = _clients['project_manager']
      if (!client) { bucket('project_manager', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("zip_upload_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('project_manager', outcome)
    })
    it('owner / read', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      const { data, error } = await client.from("zip_upload_jobs").select('*').limit(1)
      const outcome = classify('read', data, error)
      bucket('owner', outcome)
    })
    it('owner / insert', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Empty payload — relies on column defaults. PG will reject if NOT
      // NULL columns lack defaults; that error is classified as 'deny'.
      const { data, error } = await client.from("zip_upload_jobs").insert({}).select('id').limit(1)
      const outcome = classify('insert', data, error)
      bucket('owner', outcome)
      // Cleanup any successfully inserted row (defensive).
      if (outcome === 'allow' && Array.isArray(data) && data.length > 0) {
        const inserted = data[0] as { id?: string }
        if (inserted?.id) {
          await admin().from("zip_upload_jobs").delete().eq('id', inserted.id)
        }
      }
    })
    it('owner / update', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Update with a no-op filter that matches nothing real → any rows
      // returned mean the policy let us through. Using an impossible id
      // means we never actually mutate data; the policy boundary is what
      // gets exercised.
      const { data, error } = await client.from("zip_upload_jobs").update({}).eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('update', data, error)
      bucket('owner', outcome)
    })
    it('owner / delete', async () => {
      const client = _clients['owner']
      if (!client) { bucket('owner', 'skip'); return }
      // Same impossible-id pattern as update — never mutates real data.
      const { data, error } = await client.from("zip_upload_jobs").delete().eq('id', '00000000-0000-0000-0000-000000000000').select('id').limit(1)
      const outcome = classify('delete', data, error)
      bucket('owner', outcome)
    })
  })
})

// Emit human-readable per-role summary after the whole suite runs.
// (vitest's afterAll fires once at the end of the file.)
afterAll(() => {
  if (!SHOULD_RUN) return
  console.log('\n══ B.5 RLS role-matrix — per-role outcomes ══')
  for (const role of ROLES) {
    const r = _results[role]
    console.log(`  ${role.padEnd(16)} allow=${r.allow}  deny=${r.deny}  error=${r.error}  skip=${r.skip}`)
  }
})
