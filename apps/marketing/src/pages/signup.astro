---
// Marketing-side /signup is a redirect to the actual app sign-up page.
// Astro handles this with a meta refresh + JS redirect for fastest UX.

const target = 'https://app.sitesyncai.com/signup';
const url = new URL(Astro.url);
const params = url.searchParams.toString();
const targetWithParams = params.length > 0 ? `${target}?${params}` : target;
---
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Redirecting to sign up — SiteSync</title>
  <meta http-equiv="refresh" content={`0; url=${targetWithParams}`} />
  <meta name="robots" content="noindex" />
  <link rel="canonical" href={targetWithParams} />
</head>
<body>
  <p>Redirecting to <a href={targetWithParams}>sign up</a>…</p>
  <script>
    window.location.replace(document.querySelector<HTMLLinkElement>('link[rel=canonical]')!.href);
  </script>
</body>
</html>
